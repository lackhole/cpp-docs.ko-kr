---
title: CRichEditCtrl 클래스
ms.date: 11/04/2016
f1_keywords:
- CRichEditCtrl
- AFXCMN/CRichEditCtrl
- AFXCMN/CRichEditCtrl::CRichEditCtrl
- AFXCMN/CRichEditCtrl::CanPaste
- AFXCMN/CRichEditCtrl::CanRedo
- AFXCMN/CRichEditCtrl::CanUndo
- AFXCMN/CRichEditCtrl::CharFromPos
- AFXCMN/CRichEditCtrl::Clear
- AFXCMN/CRichEditCtrl::Copy
- AFXCMN/CRichEditCtrl::Create
- AFXCMN/CRichEditCtrl::CreateEx
- AFXCMN/CRichEditCtrl::Cut
- AFXCMN/CRichEditCtrl::DisplayBand
- AFXCMN/CRichEditCtrl::EmptyUndoBuffer
- AFXCMN/CRichEditCtrl::FindText
- AFXCMN/CRichEditCtrl::FindWordBreak
- AFXCMN/CRichEditCtrl::FormatRange
- AFXCMN/CRichEditCtrl::GetCharPos
- AFXCMN/CRichEditCtrl::GetDefaultCharFormat
- AFXCMN/CRichEditCtrl::GetEventMask
- AFXCMN/CRichEditCtrl::GetFirstVisibleLine
- AFXCMN/CRichEditCtrl::GetIRichEditOle
- AFXCMN/CRichEditCtrl::GetLimitText
- AFXCMN/CRichEditCtrl::GetLine
- AFXCMN/CRichEditCtrl::GetLineCount
- AFXCMN/CRichEditCtrl::GetModify
- AFXCMN/CRichEditCtrl::GetOptions
- AFXCMN/CRichEditCtrl::GetParaFormat
- AFXCMN/CRichEditCtrl::GetPunctuation
- AFXCMN/CRichEditCtrl::GetRect
- AFXCMN/CRichEditCtrl::GetRedoName
- AFXCMN/CRichEditCtrl::GetSel
- AFXCMN/CRichEditCtrl::GetSelectionCharFormat
- AFXCMN/CRichEditCtrl::GetSelectionType
- AFXCMN/CRichEditCtrl::GetSelText
- AFXCMN/CRichEditCtrl::GetTextLength
- AFXCMN/CRichEditCtrl::GetTextLengthEx
- AFXCMN/CRichEditCtrl::GetTextMode
- AFXCMN/CRichEditCtrl::GetTextRange
- AFXCMN/CRichEditCtrl::GetUndoName
- AFXCMN/CRichEditCtrl::GetWordWrapMode
- AFXCMN/CRichEditCtrl::HideSelection
- AFXCMN/CRichEditCtrl::LimitText
- AFXCMN/CRichEditCtrl::LineFromChar
- AFXCMN/CRichEditCtrl::LineIndex
- AFXCMN/CRichEditCtrl::LineLength
- AFXCMN/CRichEditCtrl::LineScroll
- AFXCMN/CRichEditCtrl::Paste
- AFXCMN/CRichEditCtrl::PasteSpecial
- AFXCMN/CRichEditCtrl::PosFromChar
- AFXCMN/CRichEditCtrl::Redo
- AFXCMN/CRichEditCtrl::ReplaceSel
- AFXCMN/CRichEditCtrl::RequestResize
- AFXCMN/CRichEditCtrl::SetAutoURLDetect
- AFXCMN/CRichEditCtrl::SetBackgroundColor
- AFXCMN/CRichEditCtrl::SetDefaultCharFormat
- AFXCMN/CRichEditCtrl::SetEventMask
- AFXCMN/CRichEditCtrl::SetModify
- AFXCMN/CRichEditCtrl::SetOLECallback
- AFXCMN/CRichEditCtrl::SetOptions
- AFXCMN/CRichEditCtrl::SetParaFormat
- AFXCMN/CRichEditCtrl::SetPunctuation
- AFXCMN/CRichEditCtrl::SetReadOnly
- AFXCMN/CRichEditCtrl::SetRect
- AFXCMN/CRichEditCtrl::SetSel
- AFXCMN/CRichEditCtrl::SetSelectionCharFormat
- AFXCMN/CRichEditCtrl::SetTargetDevice
- AFXCMN/CRichEditCtrl::SetTextMode
- AFXCMN/CRichEditCtrl::SetUndoLimit
- AFXCMN/CRichEditCtrl::SetWordCharFormat
- AFXCMN/CRichEditCtrl::SetWordWrapMode
- AFXCMN/CRichEditCtrl::StopGroupTyping
- AFXCMN/CRichEditCtrl::StreamIn
- AFXCMN/CRichEditCtrl::StreamOut
- AFXCMN/CRichEditCtrl::Undo
helpviewer_keywords:
- CRichEditCtrl [MFC], CRichEditCtrl
- CRichEditCtrl [MFC], CanPaste
- CRichEditCtrl [MFC], CanRedo
- CRichEditCtrl [MFC], CanUndo
- CRichEditCtrl [MFC], CharFromPos
- CRichEditCtrl [MFC], Clear
- CRichEditCtrl [MFC], Copy
- CRichEditCtrl [MFC], Create
- CRichEditCtrl [MFC], CreateEx
- CRichEditCtrl [MFC], Cut
- CRichEditCtrl [MFC], DisplayBand
- CRichEditCtrl [MFC], EmptyUndoBuffer
- CRichEditCtrl [MFC], FindText
- CRichEditCtrl [MFC], FindWordBreak
- CRichEditCtrl [MFC], FormatRange
- CRichEditCtrl [MFC], GetCharPos
- CRichEditCtrl [MFC], GetDefaultCharFormat
- CRichEditCtrl [MFC], GetEventMask
- CRichEditCtrl [MFC], GetFirstVisibleLine
- CRichEditCtrl [MFC], GetIRichEditOle
- CRichEditCtrl [MFC], GetLimitText
- CRichEditCtrl [MFC], GetLine
- CRichEditCtrl [MFC], GetLineCount
- CRichEditCtrl [MFC], GetModify
- CRichEditCtrl [MFC], GetOptions
- CRichEditCtrl [MFC], GetParaFormat
- CRichEditCtrl [MFC], GetPunctuation
- CRichEditCtrl [MFC], GetRect
- CRichEditCtrl [MFC], GetRedoName
- CRichEditCtrl [MFC], GetSel
- CRichEditCtrl [MFC], GetSelectionCharFormat
- CRichEditCtrl [MFC], GetSelectionType
- CRichEditCtrl [MFC], GetSelText
- CRichEditCtrl [MFC], GetTextLength
- CRichEditCtrl [MFC], GetTextLengthEx
- CRichEditCtrl [MFC], GetTextMode
- CRichEditCtrl [MFC], GetTextRange
- CRichEditCtrl [MFC], GetUndoName
- CRichEditCtrl [MFC], GetWordWrapMode
- CRichEditCtrl [MFC], HideSelection
- CRichEditCtrl [MFC], LimitText
- CRichEditCtrl [MFC], LineFromChar
- CRichEditCtrl [MFC], LineIndex
- CRichEditCtrl [MFC], LineLength
- CRichEditCtrl [MFC], LineScroll
- CRichEditCtrl [MFC], Paste
- CRichEditCtrl [MFC], PasteSpecial
- CRichEditCtrl [MFC], PosFromChar
- CRichEditCtrl [MFC], Redo
- CRichEditCtrl [MFC], ReplaceSel
- CRichEditCtrl [MFC], RequestResize
- CRichEditCtrl [MFC], SetAutoURLDetect
- CRichEditCtrl [MFC], SetBackgroundColor
- CRichEditCtrl [MFC], SetDefaultCharFormat
- CRichEditCtrl [MFC], SetEventMask
- CRichEditCtrl [MFC], SetModify
- CRichEditCtrl [MFC], SetOLECallback
- CRichEditCtrl [MFC], SetOptions
- CRichEditCtrl [MFC], SetParaFormat
- CRichEditCtrl [MFC], SetPunctuation
- CRichEditCtrl [MFC], SetReadOnly
- CRichEditCtrl [MFC], SetRect
- CRichEditCtrl [MFC], SetSel
- CRichEditCtrl [MFC], SetSelectionCharFormat
- CRichEditCtrl [MFC], SetTargetDevice
- CRichEditCtrl [MFC], SetTextMode
- CRichEditCtrl [MFC], SetUndoLimit
- CRichEditCtrl [MFC], SetWordCharFormat
- CRichEditCtrl [MFC], SetWordWrapMode
- CRichEditCtrl [MFC], StopGroupTyping
- CRichEditCtrl [MFC], StreamIn
- CRichEditCtrl [MFC], StreamOut
- CRichEditCtrl [MFC], Undo
ms.assetid: 2be52788-822c-4c27-aafd-2471231e74eb
ms.openlocfilehash: fc2b4c08b5a999f0369199e0dd65a09a7cdf8dff
ms.sourcegitcommit: fcb48824f9ca24b1f8bd37d647a4d592de1cc925
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 08/15/2019
ms.locfileid: "69503158"
---
# <a name="cricheditctrl-class"></a>CRichEditCtrl 클래스

Rich Edit 컨트롤의 기능을 제공합니다.

## <a name="syntax"></a>구문

```
class CRichEditCtrl : public CWnd
```

## <a name="members"></a>멤버

### <a name="public-constructors"></a>Public 생성자

|이름|Description|
|----------|-----------------|
|[CRichEditCtrl::CRichEditCtrl](#cricheditctrl)|`CRichEditCtrl` 개체를 생성합니다.|

### <a name="public-methods"></a>Public 메서드

|이름|Description|
|----------|-----------------|
|[CRichEditCtrl::CanPaste](#canpaste)|클립보드의 내용을이 rich edit 컨트롤에 붙여 넣을 수 있는지 여부를 결정 합니다.|
|[CRichEditCtrl::CanRedo](#canredo)|컨트롤의 redo queue에 작업이 있는지 여부를 확인 합니다.|
|[CRichEditCtrl::CanUndo](#canundo)|편집 작업을 실행 취소할 수 있는지 여부를 결정 합니다.|
|[CRichEditCtrl::CharFromPos](#charfrompos)|편집 컨트롤의 클라이언트 영역에서 지정 된 지점에 가장 가까운 문자에 대 한 정보를 검색 합니다.|
|[CRichEditCtrl::Clear](#clear)|현재 선택 영역을 지웁니다.|
|[CRichEditCtrl::Copy](#copy)|현재 선택 영역을 클립보드에 복사 합니다.|
|[CRichEditCtrl::Create](#create)|Windows rich edit 컨트롤을 만들고이 `CRichEditCtrl` 개체에 연결 합니다.|
|[CRichEditCtrl::CreateEx](#createex)|지정 된 확장 windows 스타일을 사용 하 여 Windows rich edit 컨트롤을 만들고이 `CRichEditCtrl` 개체에 연결 합니다.|
|[CRichEditCtrl::Cut](#cut)|현재 선택 영역을 클립보드로 잘라냅니다.|
|[CRichEditCtrl::DisplayBand](#displayband)|이 `CRichEditCtrl` 개체의 내용 일부를 표시 합니다.|
|[CRichEditCtrl::EmptyUndoBuffer](#emptyundobuffer)|이 `CRichEditCtrl` 개체의 실행 취소 플래그를 다시 설정 (지우기) 합니다.|
|[CRichEditCtrl::FindText](#findtext)|이 `CRichEditCtrl` 개체 내에서 텍스트를 찾습니다.|
|[CRichEditCtrl::FindWordBreak](#findwordbreak)|지정 된 문자 위치 앞뒤의 다음 단어 분리를 찾거나 해당 위치에서 문자에 대 한 정보를 검색 합니다.|
|[CRichEditCtrl::FormatRange](#formatrange)|대상 출력 장치에 대 한 텍스트 범위의 서식을 지정 합니다.|
|[CRichEditCtrl::GetCharPos](#getcharpos)|이 `CRichEditCtrl` 개체 내에서 지정 된 문자의 위치를 확인 합니다.|
|[CRichEditCtrl::GetDefaultCharFormat](#getdefaultcharformat)|이 `CRichEditCtrl` 개체의 현재 기본 문자 서식 특성을 검색 합니다.|
|[CRichEditCtrl::GetEventMask](#geteventmask)|이 `CRichEditCtrl` 개체에 대 한 이벤트 마스크를 검색 합니다.|
|[CRichEditCtrl::GetFirstVisibleLine](#getfirstvisibleline)|이 `CRichEditCtrl` 개체에서 표시 되는 맨 위 줄을 결정 합니다.|
|[CRichEditCtrl::GetIRichEditOle](#getiricheditole)|이 rich edit 컨트롤의 `IRichEditOle` 인터페이스에 대 한 포인터를 검색 합니다.|
|[CRichEditCtrl::GetLimitText](#getlimittext)|사용자가이 `CRichEditCtrl` 개체에 입력할 수 있는 텍스트 양에 대 한 한도를 가져옵니다.|
|[CRichEditCtrl::GetLine](#getline)|이 `CRichEditCtrl` 개체에서 텍스트 줄을 검색 합니다.|
|[CRichEditCtrl::GetLineCount](#getlinecount)|이 `CRichEditCtrl` 개체의 줄 수를 검색 합니다.|
|[CRichEditCtrl::GetModify](#getmodify)|이 `CRichEditCtrl` 개체의 내용이 마지막으로 저장 된 후에 변경 되었는지 여부를 확인 합니다.|
|[CRichEditCtrl::GetOptions](#getoptions)|Rich edit 컨트롤 옵션을 검색 합니다.|
|[CRichEditCtrl::GetParaFormat](#getparaformat)|이 `CRichEditCtrl` 개체의 현재 선택 영역에 있는 단락 서식 특성을 검색 합니다.|
|[CRichEditCtrl::GetPunctuation](#getpunctuation)|Rich edit 컨트롤의 현재 문장 부호 문자를 검색 합니다. 이 메시지는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.|
|[CRichEditCtrl::GetRect](#getrect)|이 `CRichEditCtrl` 개체의 서식 지정 사각형을 검색 합니다.|
|[CRichEditCtrl::GetRedoName](#getredoname)|컨트롤의 redo queue에서 다음 작업의 형식 (있는 경우)을 검색 합니다.|
|[CRichEditCtrl::GetSel](#getsel)|이 `CRichEditCtrl` 개체에서 현재 선택 된의 시작 및 끝 위치를 가져옵니다.|
|[CRichEditCtrl::GetSelectionCharFormat](#getselectioncharformat)|이 `CRichEditCtrl` 개체의 현재 선택 영역에 있는 문자 서식 특성을 검색 합니다.|
|[CRichEditCtrl::GetSelectionType](#getselectiontype)|이 `CRichEditCtrl` 개체의 현재 선택 영역에 있는 콘텐츠 형식을 검색 합니다.|
|[CRichEditCtrl::GetSelText](#getseltext)|이 `CRichEditCtrl` 개체의 현재 선택 영역에 대 한 텍스트를 가져옵니다.|
|[CRichEditCtrl::GetTextLength](#gettextlength)|이 `CRichEditCtrl` 개체에서 텍스트의 길이 (문자)를 검색 합니다. 는 null 종결 문자를 포함 하지 않습니다.|
|[CRichEditCtrl::GetTextLengthEx](#gettextlengthex)|Rich edit 뷰의 문자 또는 바이트 수를 검색 합니다. Rich edit 컨트롤의 텍스트 길이를 결정 하는 방법을 나타내는 플래그 목록을 수락 합니다.|
|[CRichEditCtrl::GetTextMode](#gettextmode)|Rich edit 컨트롤의 현재 텍스트 모드와 실행 취소 수준을 검색 합니다.|
|[CRichEditCtrl::GetTextRange](#gettextrange)|지정 된 텍스트 범위를 검색 합니다.|
|[CRichEditCtrl::GetUndoName](#getundoname)|다음 실행 취소 작업의 형식 (있는 경우)을 검색 합니다.|
|[CRichEditCtrl::GetWordWrapMode](#getwordwrapmode)|Rich edit 컨트롤의 현재 단어 줄 바꿈 및 단어 분리 옵션을 검색 합니다. 이 메시지는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.|
|[CRichEditCtrl::HideSelection](#hideselection)|현재 선택 영역을 표시 하거나 숨깁니다.|
|[CRichEditCtrl::LimitText](#limittext)|사용자가 `CRichEditCtrl` 개체에 입력할 수 있는 텍스트의 양을 제한 합니다.|
|[CRichEditCtrl::LineFromChar](#linefromchar)|지정 된 문자를 포함 하는 줄을 결정 합니다.|
|[CRichEditCtrl::LineIndex](#lineindex)|이 `CRichEditCtrl` 개체에서 지정 된 줄의 문자 인덱스를 검색 합니다.|
|[CRichEditCtrl::LineLength](#linelength)|이 `CRichEditCtrl` 개체에서 지정 된 줄의 길이를 검색 합니다.|
|[CRichEditCtrl::LineScroll](#linescroll)|이 `CRichEditCtrl` 개체의 텍스트를 스크롤합니다.|
|[CRichEditCtrl::Paste](#paste)|클립보드의 내용을이 rich edit 컨트롤에 삽입 합니다.|
|[CRichEditCtrl::PasteSpecial](#pastespecial)|클립보드의 내용을 지정 된 데이터 형식의 rich edit 컨트롤에 삽입 합니다.|
|[CRichEditCtrl::PosFromChar](#posfromchar)|편집 컨트롤에서 지정 된 문자의 클라이언트 영역 좌표를 검색 합니다.|
|[CRichEditCtrl::Redo](#redo)|컨트롤의 redo queue에서 다음 작업을 다시 실행 합니다.|
|[CRichEditCtrl::ReplaceSel](#replacesel)|이 `CRichEditCtrl` 개체의 현재 선택 영역을 지정 된 텍스트로 바꿉니다.|
|[CRichEditCtrl::RequestResize](#requestresize)|이 `CRichEditCtrl` 개체가 요청 크기 조정 알림을 보내도록 합니다.|
|[CRichEditCtrl::SetAutoURLDetect](#setautourldetect)|서식 있는 편집 컨트롤에서 자동 URL 검색이 활성화 되어 있는지 여부를 나타냅니다.|
|[CRichEditCtrl::SetBackgroundColor](#setbackgroundcolor)|이 `CRichEditCtrl` 개체의 배경색을 설정 합니다.|
|[CRichEditCtrl::SetDefaultCharFormat](#setdefaultcharformat)|이 `CRichEditCtrl` 개체의 현재 기본 문자 서식 특성을 설정 합니다.|
|[CRichEditCtrl::SetEventMask](#seteventmask)|이 `CRichEditCtrl` 개체에 대 한 이벤트 마스크를 설정 합니다.|
|[CRichEditCtrl::SetModify](#setmodify)|이 `CRichEditCtrl` 개체에 대 한 수정 플래그를 설정 하거나 취소 합니다.|
|[CRichEditCtrl::SetOLECallback](#setolecallback)|이 rich `IRichEditOleCallback` edit 컨트롤의 COM 개체를 설정 합니다.|
|[CRichEditCtrl::SetOptions](#setoptions)|이 `CRichEditCtrl` 개체에 대 한 옵션을 설정 합니다.|
|[CRichEditCtrl::SetParaFormat](#setparaformat)|이 `CRichEditCtrl` 개체의 현재 선택 영역에 있는 단락 서식 특성을 설정 합니다.|
|[CRichEditCtrl::SetPunctuation](#setpunctuation)|Rich edit 컨트롤의 문장 부호 문자를 설정 합니다. 이 메시지는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.|
|[CRichEditCtrl::SetReadOnly](#setreadonly)|이 `CRichEditCtrl` 개체에 대 한 읽기 전용 옵션을 설정 합니다.|
|[CRichEditCtrl::SetRect](#setrect)|이 `CRichEditCtrl` 개체의 서식 지정 사각형을 설정 합니다.|
|[CRichEditCtrl::SetSel](#setsel)|이 `CRichEditCtrl` 개체의 선택 항목을 설정 합니다.|
|[CRichEditCtrl::SetSelectionCharFormat](#setselectioncharformat)|이 `CRichEditCtrl` 개체의 현재 선택 영역에 있는 문자 서식 특성을 설정 합니다.|
|[CRichEditCtrl::SetTargetDevice](#settargetdevice)|이 `CRichEditCtrl` 개체에 대 한 대상 출력 장치를 설정 합니다.|
|[CRichEditCtrl::SetTextMode](#settextmode)|Rich edit 컨트롤의 텍스트 모드 또는 실행 취소 수준을 설정 합니다. 컨트롤에 텍스트가 포함 되어 있으면 메시지가 실패 합니다.|
|[CRichEditCtrl::SetUndoLimit](#setundolimit)|실행 취소 큐에 저장 될 수 있는 최대 작업 수를 설정 합니다.|
|[CRichEditCtrl::SetWordCharFormat](#setwordcharformat)|이 `CRichEditCtrl` 개체의 현재 단어에서 문자 서식 특성을 설정 합니다.|
|[CRichEditCtrl::SetWordWrapMode](#setwordwrapmode)|Rich edit 컨트롤의 단어 줄 바꿈 및 단어 분리 옵션을 설정 합니다. 이 메시지는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.|
|[CRichEditCtrl::StopGroupTyping](#stopgrouptyping)|컨트롤에서 추가 입력 작업을 현재 실행 취소 작업으로 수집 하지 않도록 합니다. 컨트롤은 다음 입력 작업 (있는 경우)을 실행 취소 큐의 새 작업에 저장 합니다.|
|[CRichEditCtrl::StreamIn](#streamin)|입력 스트림의 텍스트를이 `CRichEditCtrl` 개체에 삽입 합니다.|
|[CRichEditCtrl::StreamOut](#streamout)|이 `CRichEditCtrl` 개체의 텍스트를 출력 스트림에 저장 합니다.|
|[CRichEditCtrl::Undo](#undo)|마지막 편집 작업을 취소 합니다.|

## <a name="remarks"></a>설명

"Rich edit 컨트롤"은 사용자가 텍스트를 입력 하 고 편집할 수 있는 창입니다. 텍스트에는 문자 및 단락 서식 지정이 할당 될 수 있으며 포함 된 OLE 개체도 포함 될 수 있습니다. Rich edit 컨트롤은 텍스트 서식 지정을 위한 프로그래밍 인터페이스를 제공 합니다. 그러나 응용 프로그램은 사용자가 서식 지정 작업을 사용 하도록 설정 하는 데 필요한 모든 사용자 인터페이스 구성 요소를 구현 해야 합니다.

이 windows 공용 컨트롤 (및 `CRichEditCtrl` 클래스)은 windows 95/98 및 windows NT 버전 3.51 이상에서 실행 되는 프로그램에만 사용할 수 있습니다. 클래스 `CRichEditCtrl` 는 Windows SDK rich edit 컨트롤의 버전 2.0 및 3.0을 지원 합니다.

> [!CAUTION]
>  응용 프로그램이 SDI, MDI 또는 대화 상자 기반 인지에 관계 없이 대화 상자에서 rich edit 컨트롤을 사용 하는 경우 대화 상자가 표시 되기 전에 [AfxInitRichEdit](application-information-and-management.md#afxinitrichedit) 를 한 번 호출 해야 합니다. 이 함수를 호출 하는 일반적인 장소는 프로그램의 `InitInstance` 멤버 함수에 있습니다. 이 대화 상자는 처음에만 표시할 때마다 호출할 필요가 없습니다. 를 사용 `AfxInitRichEdit` `CRichEditView`하는 경우를 호출할 필요가 없습니다.

사용 `CRichEditCtrl`에 대 한 자세한 내용은 다음을 참조 하세요.

- [컨트롤](../../mfc/controls-mfc.md)

- [CRichEditCtrl 사용](../../mfc/using-cricheditctrl.md)

MFC 응용 프로그램에서 rich edit 컨트롤을 사용 하는 예제는 [워드 패드](../../overview/visual-cpp-samples.md) 샘플 응용 프로그램을 참조 하세요.

## <a name="inheritance-hierarchy"></a>상속 계층 구조

[CObject](../../mfc/reference/cobject-class.md)

[CCmdTarget](../../mfc/reference/ccmdtarget-class.md)

[CWnd](../../mfc/reference/cwnd-class.md)

`CRichEditCtrl`

## <a name="requirements"></a>요구 사항

**헤더:** afxcmn.h

##  <a name="canpaste"></a>  CRichEditCtrl::CanPaste

Rich edit 컨트롤이 지정 된 클립보드 형식을 붙여 넣을 수 있는지 여부를 결정 합니다.

```
BOOL CanPaste(UINT nFormat = 0) const;
```

### <a name="parameters"></a>매개 변수

*nFormat*<br/>
쿼리할 클립보드 데이터 형식입니다. 이 매개 변수는 미리 정의 된 클립보드 형식 또는 [RegisterClipboardFormat](/windows/win32/api/winuser/nf-winuser-registerclipboardformatw)에서 반환 되는 값 중 하나일 수 있습니다.

### <a name="return-value"></a>반환 값

클립보드 형식을 붙여넣을 수 있는 경우 0이 아닌 값입니다. 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

*Nformat* 이 0 `CanPaste` 인 경우는 현재 클립보드에 있는 모든 형식을 시도 합니다.

자세한 내용은 Windows SDK [EM_CANPASTE](/windows/win32/Controls/em-canpaste) Message and [RegisterClipboardFormat](/windows/win32/api/winuser/nf-winuser-registerclipboardformatw) function을 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#1](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_1.cpp)]

##  <a name="canredo"></a>  CRichEditCtrl::CanRedo

Redo queue에 작업이 포함 되어 있는지 여부를 확인 합니다.

```
BOOL CanRedo() const;
```

### <a name="return-value"></a>반환 값

Redo queue에 작업이 포함 되어 있으면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

Redo queue에서 작업 이름을 검색 하려면 [CRichEditCtrl:: GetRedoName](#getredoname)를 호출 합니다. 가장 최근의 실행 취소 작업을 다시 실행 하려면 [redo](#redo)를 호출 합니다.

자세한 내용은 Windows SDK에서 [EM_CANREDO](/windows/win32/Controls/em-canredo) 을 참조 하세요.

##  <a name="canundo"></a>  CRichEditCtrl::CanUndo

마지막 편집 작업을 실행 취소할 수 있는지 여부를 결정 합니다.

```
BOOL CanUndo() const;
```

### <a name="return-value"></a>반환 값

[Undo](#undo) 멤버 함수를 호출 하 여 마지막 편집 작업을 실행 취소할 수 있는 경우 0이 아닙니다. 실행 취소할 수 없는 경우 0입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_CANUNDO](/windows/win32/Controls/em-canundo) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#2](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_2.cpp)]

##  <a name="charfrompos"></a>  CRichEditCtrl::CharFromPos

매개 변수 *pt*로 지정 된 지점에 있는 문자에 대 한 정보를 검색 합니다.

```
int CharFromPos(CPoint pt) const;
```

### <a name="parameters"></a>매개 변수

*pt*<br/>
지정 된 점의 좌표를 포함 하는 [cpoint](../../atl-mfc-shared/reference/cpoint-class.md) 개체입니다.

### <a name="return-value"></a>반환 값

지정 된 지점에 가장 가까운 문자의 0부터 시작 하는 문자 인덱스입니다. 지정 된 점이 컨트롤의 마지막 문자를 넘으면 반환 값은 컨트롤의 마지막 문자를 나타냅니다.

### <a name="remarks"></a>설명

이 멤버 함수는 rich edit 컨트롤에서 작동 합니다. 편집 컨트롤에 대 한 정보를 가져오려면 [CEdit:: CharFromPos](../../mfc/reference/cedit-class.md#charfrompos)를 호출 합니다.

자세한 내용은 Windows SDK에서 [EM_CHARFROMPOS](/windows/win32/Controls/em-charfrompos) 을 참조 하세요.

##  <a name="clear"></a>  CRichEditCtrl::Clear

Rich edit 컨트롤에서 현재 선택 영역 (있는 경우)을 삭제 (선택 취소) 합니다.

```
void Clear();
```

### <a name="remarks"></a>설명

[실행 취소](#undo) 멤버 함수 `Clear` 를 호출 하 여에서 수행 하는 삭제 작업을 취소할 수 있습니다.

현재 선택 영역을 삭제 하 고 삭제 된 내용을 클립보드에 배치 하려면 [Cut](#cut) 멤버 함수를 호출 합니다.

자세한 내용은 Windows SDK에서 [WM_CLEAR](/windows/win32/dataxchg/wm-clear) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#3](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_3.cpp)]

##  <a name="copy"></a>  CRichEditCtrl::Copy

Rich edit 컨트롤의 현재 선택 영역 (있는 경우)을 클립보드에 복사 합니다.

```
void Copy();
```

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [WM_COPY](/windows/win32/dataxchg/wm-copy) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#4](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_4.cpp)]

##  <a name="create"></a>  CRichEditCtrl::Create

Windows rich edit 컨트롤을 만들고이 `CRichEditCtrl` 개체에 연결 합니다.

```
virtual BOOL Create(
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID);
```

### <a name="parameters"></a>매개 변수

*dwStyle*<br/>
편집 컨트롤의 스타일을 지정 합니다. 아래의 **설명** 섹션에 나열 된 창 스타일의 조합을 적용 하 고 Windows SDK에 설명 된 [컨트롤 스타일을 편집](/windows/win32/Controls/edit-control-styles)합니다.

*rect*<br/>
편집 컨트롤의 크기와 위치를 지정 합니다. 는 [Crect](../../atl-mfc-shared/reference/crect-class.md) 개체 또는 [RECT](/windows/win32/api/windef/ns-windef-rect) 구조 일 수 있습니다.

*pParentWnd*<br/>
편집 컨트롤의 부모 창 (종종 [CDialog](../../mfc/reference/cdialog-class.md))을 지정 합니다. NULL이 아니어야 합니다.

*nID*<br/>
편집 컨트롤의 ID를 지정 합니다.

### <a name="return-value"></a>반환 값

초기화에 성공 하면 0이 아닌 값입니다. 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

두 단계로 개체 `CRichEditCtrl` 를 구성 합니다. 먼저 [CRichEditCtrl](#cricheditctrl) 생성자를 호출한 다음을 호출 `Create`하 여 Windows 편집 컨트롤을 만들고이를 `CRichEditCtrl` 개체에 연결 합니다.

이 함수를 사용 하 여 rich edit 컨트롤을 만드는 경우 먼저 필요한 공용 컨트롤 라이브러리를 로드 해야 합니다. 라이브러리를 로드 하려면 전역 함수 [AfxInitRichEdit](application-information-and-management.md#afxinitrichedit)를 호출 합니다. 그러면 공용 컨트롤 라이브러리를 초기화 합니다. 프로세스에서를 한 `AfxInitRichEdit` 번만 호출 해야 합니다.

가 `Create` 실행 되 면 Windows에서 [WM_NCCREATE](../../mfc/reference/cwnd-class.md#onnccreate), [WM_NCCALCSIZE](../../mfc/reference/cwnd-class.md#onnccalcsize), [WM_CREATE](../../mfc/reference/cwnd-class.md#oncreate)및 [WM_GETMINMAXINFO](../../mfc/reference/cwnd-class.md#ongetminmaxinfo) 메시지를 편집 컨트롤로 보냅니다.

`CWnd` 이러한 메시지는 기본적으로 기본 클래스의 [OnNcCreate](../../mfc/reference/cwnd-class.md#onnccreate), [OnNcCalcSize](../../mfc/reference/cwnd-class.md#onnccalcsize), [OnCreate](../../mfc/reference/cwnd-class.md#oncreate)및 [OnGetMinMaxInfo](../../mfc/reference/cwnd-class.md#ongetminmaxinfo) 멤버 함수에 의해 처리 됩니다. 기본 메시지 처리를 확장 하려면에서 `CRichEditCtrl`클래스를 파생 시키고 메시지 맵을 새 클래스에 추가 하 고 위의 메시지 처리기 멤버 함수를 재정의 합니다. 예 `OnCreate`를 들어를 재정의 하 여 새 클래스에 필요한 초기화를 수행 합니다.

다음 [창 스타일](../../mfc/reference/styles-used-by-mfc.md#window-styles) 을 편집 컨트롤에 적용 합니다.

- WS_CHILD는 항상입니다.

- WS_VISIBLE는 일반적으로입니다.

- 거의 WS_DISABLED.

- WS_GROUP 컨트롤을 그룹화 합니다.

- WS_TABSTOP는 탭 이동 순서에 편집 컨트롤을 포함 합니다.

창 스타일에 대 한 자세한 내용은 Windows SDK의 [CreateWindow](/windows/win32/api/winuser/nf-winuser-createwindoww) 를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#5](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_5.cpp)]

##  <a name="createex"></a>  CRichEditCtrl::CreateEx

컨트롤 (자식 창)을 만들고이 `CRichEditCtrl` 를 개체에 연결 합니다.

```
virtual BOOL CreateEx(
    DWORD dwExStyle,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID);
```

### <a name="parameters"></a>매개 변수

*dwExStyle*<br/>
만들려는 컨트롤의 확장 스타일을 지정 합니다. 확장 된 Windows 스타일의 목록에 대해서는 Windows SDK의 [CreateWindowEx](/windows/win32/api/winuser/nf-winuser-createwindowexw) 에 대 한 *dwexstyle* 매개 변수를 참조 하세요.

*dwStyle*<br/>
편집 컨트롤의 스타일을 지정 합니다. Windows SDK에서 설명 하는 [컨트롤 스타일](/windows/win32/Controls/edit-control-styles) [만들기](#create) 및 편집의 **설명** 섹션에 나열 된 창 스타일의 조합을 적용 합니다.

*rect*<br/>
*PParentWnd*의 클라이언트 좌표에서 만들 창의 크기와 위치를 설명 하는 [RECT](/previous-versions/dd162897\(v=vs.85\)) 구조체에 대 한 참조입니다.

*pParentWnd*<br/>
컨트롤의 부모 창에 대 한 포인터입니다.

*nID*<br/>
컨트롤의 자식 창 ID입니다.

### <a name="return-value"></a>반환 값

성공하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

대신를 사용 `CreateEx`하 여 windows 확장 스타일 앞에 WS_EX_ 지정 된 확장 된 windows 스타일을 적용 합니다 `Create` .

##  <a name="cricheditctrl"></a>  CRichEditCtrl::CRichEditCtrl

`CRichEditCtrl` 개체를 생성합니다.

```
CRichEditCtrl();
```

### <a name="remarks"></a>설명

[Create](#create) 를 사용 하 여 Windows rich edit 컨트롤을 생성 합니다.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#6](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_6.cpp)]

##  <a name="cut"></a>  CRichEditCtrl::Cut

Rich edit 컨트롤에서 현재 선택 영역 (있는 경우)을 삭제 하 고 삭제 된 텍스트를 클립보드에 복사 합니다.

```
void Cut();
```

### <a name="remarks"></a>설명

[실행 취소](#undo) 멤버 함수 `Cut` 를 호출 하 여에서 수행 하는 삭제 작업을 취소할 수 있습니다.

삭제 된 텍스트를 클립보드에 넣지 않고 현재 선택 영역을 삭제 하려면 [Clear](#clear) 멤버 함수를 호출 합니다.

자세한 내용은 Windows SDK에서 [WM_CUT](/windows/win32/dataxchg/wm-cut) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#7](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_7.cpp)]

##  <a name="displayband"></a>  CRichEditCtrl::DisplayBand

이전에 [Formatrange](#formatrange)로 서식이 지정 된 rich edit 컨트롤 (텍스트 및 OLE 항목)의 내용 일부를 표시 합니다.

```
BOOL DisplayBand(LPRECT pDisplayRect);
```

### <a name="parameters"></a>매개 변수

*pDisplayRect*<br/>
텍스트를 표시할 장치의 영역을 지정 하는 [RECT](/windows/win32/api/windef/ns-windef-rect) 또는 [crect](../../atl-mfc-shared/reference/crect-class.md) 개체에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

서식이 지정 된 텍스트의 표시가 성공 하면 0이 아닌 값이 고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

텍스트 및 OLE 항목이 *Pdisplayrect*포인터로 지정 된 영역으로 잘립니다.

자세한 내용은 Windows SDK에서 [EM_DISPLAYBAND](/windows/win32/Controls/em-displayband) 을 참조 하세요.

### <a name="example"></a>예제

  [CRichEditCtrl:: FormatRange](#formatrange)의 예제를 참조 하세요.

##  <a name="emptyundobuffer"></a>  CRichEditCtrl::EmptyUndoBuffer

이 rich edit 컨트롤의 실행 취소 플래그를 다시 설정 합니다 (clear).

```
void EmptyUndoBuffer();
```

### <a name="remarks"></a>설명

이제 컨트롤에서 마지막 편집 작업을 실행 취소할 수 없습니다. 실행 취소 플래그는 rich edit 컨트롤 내의 작업을 실행 취소할 수 있을 때마다 설정 됩니다.

실행 취소 플래그는 [CWnd](../../mfc/reference/cwnd-class.md) 멤버 함수 [setwindowtext](../../mfc/reference/cwnd-class.md#setwindowtext)를 호출할 때마다 자동으로 지워집니다.

자세한 내용은 Windows SDK에서 [EM_EMPTYUNDOBUFFER](/windows/win32/Controls/em-emptyundobuffer) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#8](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_8.cpp)]

##  <a name="findtext"></a>  CRichEditCtrl::FindText

Rich edit 컨트롤에서 텍스트를 찾습니다.

```
long FindText(
    DWORD dwFlags,
    FINDTEXTEX* pFindText) const;
```

### <a name="parameters"></a>매개 변수

*dwFlags*<br/>
가능한 값 목록은 Windows SDK의 *wParam* in [EM_FINDTEXTEXT](/windows/win32/Controls/em-findtextex) 을 참조 하십시오.

*pFindText*<br/>
검색에 대 한 매개 변수를 제공 하 고 일치 항목이 발견 된 범위를 반환 하는 [Findtextex](/windows/win32/api/richedit/ns-richedit-findtextexw) 구조에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

다음 일치 항목의 0부터 시작 하는 문자 위치입니다. 더 이상 일치 하는 항목이 없으면-1입니다.

### <a name="remarks"></a>설명

`FINDTEXTEX` 구조 내의 [charrange](/windows/win32/api/richedit/ns-richedit-charrange) 구조에서 적절 한 범위 매개 변수를 설정 하 여 위나 아래로 검색할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_FINDTEXTEX](/windows/win32/Controls/em-findtextex) Message 및 [Findtextex](/windows/win32/api/richedit/ns-richedit-findtextexw) 구조체를 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#9](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_9.cpp)]

##  <a name="findwordbreak"></a>  CRichEditCtrl::FindWordBreak

*N*으로 지정 된 위치 앞 이나 뒤의 다음 단어 분리를 찾습니다.

```
DWORD FindWordBreak(
    UINT nCode,
    DWORD nStart) const;
```

### <a name="parameters"></a>매개 변수

*nCode*<br/>
수행할 동작을 나타냅니다. 가능한 값 목록은 Windows SDK에서 EM_FINDWORDBREAK의 매개 변수 *코드* 에 대 한 설명을 참조 하세요.

*nStart*<br/>
시작할 문자 위치 (0부터 시작)입니다.

### <a name="return-value"></a>반환 값

매개 변수 *Ncode*를 기준으로 합니다. 자세한 내용은 Windows SDK에서 [EM_FINDWORDBREAK](/windows/win32/Controls/em-findwordbreak) 을 참조 하세요.

### <a name="remarks"></a>설명

이 멤버 함수를 사용 하 여 지정 된 위치에 있는 문자에 대 한 정보를 검색할 수 있습니다.

##  <a name="formatrange"></a>  CRichEditCtrl::FormatRange

특정 장치에 대해 rich edit 컨트롤의 텍스트 범위를 지정 합니다.

```
long FormatRange(
    FORMATRANGE* pfr,
    BOOL bDisplay = TRUE);
```

### <a name="parameters"></a>매개 변수

*pfr*<br/>
출력 장치에 대 한 정보를 포함 하는 [Formatrange](/windows/win32/api/richedit/ns-richedit-formatrange) 구조에 대 한 포인터입니다. NULL은 rich edit 컨트롤 내에서 캐시 된 정보를 해제할 수 있음을 나타냅니다.

*bDisplay*<br/>
텍스트를 렌더링 해야 하는지 여부를 나타냅니다. FALSE 이면 텍스트를 측정 합니다.

### <a name="return-value"></a>반환 값

영역에 맞는 마지막 문자의 인덱스 + 1입니다.

### <a name="remarks"></a>설명

일반적으로이 호출은 [Displayband](#displayband)에 대 한 호출 뒤에 나옵니다.

자세한 내용은 Windows SDK [EM_FORMATRANGE](/windows/win32/Controls/em-formatrange) Message And [formatrange](/windows/win32/api/richedit/ns-richedit-formatrange) structure 항목을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#10](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_10.cpp)]

##  <a name="getcharpos"></a>  CRichEditCtrl::GetCharPos

이 `CRichEditCtrl` 개체 내에서 지정 된 문자의 위치 (왼쪽 위 모퉁이)를 가져옵니다.

```
CPoint GetCharPos(long lChar) const;
```

### <a name="parameters"></a>매개 변수

*lChar*<br/>
문자의 0부터 시작 하는 인덱스입니다.

### <a name="return-value"></a>반환 값

*Lchar*로 지정 된 문자의 왼쪽 위 모퉁이 위치입니다.

### <a name="remarks"></a>설명

문자는 0부터 시작 하는 인덱스 값을 제공 하 여 지정 됩니다. *Lchar* 가이 `CRichEditCtrl` 개체의 마지막 문자 인덱스 보다 큰 경우 반환 값은이 개체의 마지막 문자 바로 다음 `CRichEditCtrl` 에 오는 문자 위치의 좌표를 지정 합니다.

자세한 내용은 Windows SDK에서 [EM_POSFROMCHAR](/windows/win32/Controls/em-posfromchar) 을 참조 하세요.

##  <a name="getdefaultcharformat"></a>  CRichEditCtrl::GetDefaultCharFormat

이 `CRichEditCtrl` 개체의 기본 문자 서식 특성을 가져옵니다.

```
DWORD GetDefaultCharFormat(CHARFORMAT& cf) const;  DWORD GetDefaultCharFormat(CHARFORMAT2& cf) const;
```

### <a name="parameters"></a>매개 변수

*cf*<br/>
첫 번째 버전에서 기본 문자 서식 특성을 `CHARFORMAT` 보유 하는 구조체에 대 한 포인터입니다.

두 번째 버전에서 기본 문자 서식 특성을 `CHARFORMAT2` 보유 하는 `CHARFORMAT` 구조에 대 한 풍부한 편집 2.0 확장인 구조체에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

`dwMask` *Cf*의 데이터 멤버입니다. 기본 문자 서식 특성을 지정 했습니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK EM_GETCHARFORMAT 메시지 및 `CHARFORMAT` 및 `CHARFORMAT2` 구조체를 참조 하십시오.

### <a name="example"></a>예제

  [SetDefaultCharFormat](#setdefaultcharformat)의 예제를 참조 하세요.

##  <a name="geteventmask"></a>  CRichEditCtrl::GetEventMask

이 `CRichEditCtrl` 개체에 대 한 이벤트 마스크를 가져옵니다.

```
long GetEventMask() const;
```

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체에 대 한 이벤트 마스크입니다.

### <a name="remarks"></a>설명

이벤트 마스크는 `CRichEditCtrl` 개체가 부모 창에 보내는 알림 메시지를 지정 합니다.

자세한 내용은 Windows SDK에서 [EM_GETEVENTMASK](/windows/win32/Controls/em-geteventmask) 을 참조 하세요.

### <a name="example"></a>예제

  [CRichEditCtrl:: SetEventMask](#seteventmask)의 예제를 참조 하세요.

##  <a name="getfirstvisibleline"></a>  CRichEditCtrl::GetFirstVisibleLine

이 `CRichEditCtrl` 개체에서 표시 되는 맨 위 줄을 결정 합니다.

```
int GetFirstVisibleLine() const;
```

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체에 표시 되는 맨 위 줄의 인덱스 (0부터 시작)입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_GETFIRSTVISIBLELINE](/windows/win32/Controls/em-getfirstvisibleline) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#11](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_11.cpp)]

##  <a name="getiricheditole"></a>  CRichEditCtrl::GetIRichEditOle

`IRichEditOle` 이`CRichEditCtrl` 개체의 인터페이스에 액세스 합니다.

```
IRichEditOle* GetIRichEditOle() const;
```

### <a name="return-value"></a>반환 값

이`CRichEditCtrl` 개체의 OLE 기능에 액세스 하는 데 사용할 수 있는 [IRichEditOle](/windows/win32/api/richole/nn-richole-iricheditole) 인터페이스에 대 한 포인터입니다. 인터페이스에 액세스할 수 없는 경우 NULL입니다.

### <a name="remarks"></a>설명

이 인터페이스를 사용 하 여 `CRichEditCtrl` 이 개체의 OLE 기능에 액세스 합니다.

자세한 내용은 Windows SDK [EM_GETOLEINTERFACE](/windows/win32/Controls/em-getoleinterface) Message and [IRichEditOle](/windows/win32/api/richole/nn-richole-iricheditole) interface 항목을 참조 하세요.

##  <a name="getlimittext"></a>  CRichEditCtrl::GetLimitText

이 `CRichEditCtrl` 개체의 텍스트 한도를 가져옵니다.

```
long GetLimitText() const;
```

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체의 현재 텍스트 제한 (바이트)입니다.

### <a name="remarks"></a>설명

텍스트 제한은 rich edit 컨트롤에서 허용할 수 있는 최대 텍스트 크기 (바이트)입니다.

자세한 내용은 Windows SDK에서 [EM_GETLIMITTEXT](/windows/win32/Controls/em-getlimittext) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#12](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_12.cpp)]

##  <a name="getline"></a>  CRichEditCtrl::GetLine

이 `CRichEditCtrl` 개체에서 텍스트 줄을 검색 합니다.

```
int GetLine(
    int nIndex,
    LPTSTR lpszBuffer) const;

int GetLine(
    int nIndex,
    LPTSTR lpszBuffer,
    int nMaxLength) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
검색할 줄의 인덱스 (0부터 시작)입니다.

*lpszBuffer*<br/>
텍스트를 받을 버퍼를 가리킵니다. 버퍼의 첫 번째 단어는 버퍼로 복사할 수 있는 최대 바이트 수를 지정 해야 합니다.

*nMaxLength*<br/>
*Lpszbuffer 변수가*에 복사할 수 있는 최대 문자 수입니다. 의 `GetLine` 두 번째 형태는이 값을 *lpszbuffer 변수가*로 지정 된 버퍼의 첫 번째 단어에 배치 합니다.

### <a name="return-value"></a>반환 값

*Lpszbuffer 변수가*에 복사 되는 문자 수입니다.

### <a name="remarks"></a>설명

복사한 줄에 null 종결 문자가 포함 되어 있지 않습니다.

> [!NOTE]
>  버퍼의 첫 번째 단어는 복사 될 문자 수를 저장 하므로 버퍼의 길이는 4 바이트 이상 인지 확인 합니다.

자세한 내용은 Windows SDK에서 [EM_GETLINE](/windows/win32/Controls/em-getline) 을 참조 하세요.

### <a name="example"></a>예제

  [Getlinecount](#getlinecount)의 예제를 참조 하세요.

##  <a name="getlinecount"></a>  CRichEditCtrl::GetLineCount

`CRichEditCtrl` 개체의 줄 수를 검색 합니다.

```
int GetLineCount() const;
```

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체의 줄 수입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_GETLINECOUNT](/windows/win32/Controls/em-getlinecount) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#13](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_13.cpp)]

##  <a name="getmodify"></a>  CRichEditCtrl::GetModify

이 `CRichEditCtrl` 개체의 내용이 수정 되었는지 여부를 확인 합니다.

```
BOOL GetModify() const;
```

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체의 텍스트가 수정 되었으면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

Windows는 rich edit 컨트롤의 내용이 변경 되었는지 여부를 나타내는 내부 플래그를 유지 관리 합니다. 이 플래그는 편집 컨트롤을 처음 만들 때 지워지고 [Setmodify](#setmodify) 멤버 함수를 호출 하 여 지울 수도 있습니다.

자세한 내용은 Windows SDK에서 [EM_GETMODIFY](/windows/win32/Controls/em-getmodify) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#14](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_14.cpp)]

##  <a name="getoptions"></a>  CRichEditCtrl::GetOptions

Rich edit 컨트롤에 현재 설정 된 옵션을 검색 합니다.

```
UINT GetOptions() const;
```

### <a name="return-value"></a>반환 값

현재 옵션 플래그 값의 조합입니다. 이러한 값의 목록은 Windows SDK 설명 된 대로 [EM_SETOPTIONS](/windows/win32/Controls/em-setoptions) 메시지의 *foptions* 매개 변수를 참조 하세요.

##  <a name="getparaformat"></a>  CRichEditCtrl::GetParaFormat

현재 선택 영역에 대 한 단락 서식 특성을 가져옵니다.

```
DWORD GetParaFormat(PARAFORMAT& pf) const;  DWORD GetParaFormat(PARAFORMAT2& pf) const;
```

### <a name="parameters"></a>매개 변수

*pf*<br/>
첫 번째 버전에서 현재 선택 영역에 대 한 단락 서식 특성을 유지 하는 [PARAFORMAT](/windows/win32/api/richedit/ns-richedit-paraformat) 구조체에 대 한 포인터입니다.

두 번째 버전에서 기본 문자 서식 특성을 보유 한 `PARAFORMAT` 구조에 대 한 Rich Edit 2.0 extension 인 [PARAFORMAT2](/windows/win32/api/richedit/ns-richedit-paraformat2) 구조체에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

`dwMask` *Pf*의 데이터 멤버입니다. 현재 선택 영역 전체에서 일치 하는 단락 서식 특성을 지정 합니다.

### <a name="remarks"></a>설명

두 개 이상의 단락을 선택한 경우에는 *pf* 가 첫 번째 선택한 단락의 특성을 받습니다. 반환 값은 선택 영역 전체에서 일치 하는 특성을 지정 합니다.

자세한 내용은 Windows SDK [EM_GETPARAFORMAT](/windows/win32/Controls/em-getparaformat) 메시지 및 `PARAFORMAT` 및 `PARAFORMAT2` 구조체를 참조 하십시오.

### <a name="example"></a>예제

  [CRichEditCtrl:: SetParaFormat](#setparaformat)의 예제를 참조 하세요.

##  <a name="getpunctuation"></a>  CRichEditCtrl::GetPunctuation

Rich edit 컨트롤의 현재 문장 부호 문자를 가져옵니다.

```
BOOL GetPunctuation(
    UINT fType,
    PUNCTUATION* lpPunc) const;
```

### <a name="parameters"></a>매개 변수

*fType*<br/>
Windows SDK에서 [EM_GETPUNCTUATION](/windows/win32/Controls/em-getpunctuation) 의 *fType* 매개 변수에 설명 된 대로 구두점 형식 플래그입니다.

*lpPunc*<br/>
Windows SDK에 설명 된 [문장 부호](/windows/win32/api/richedit/ns-richedit-punctuation) 구조에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

작업이 성공 하면 0이 아니고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 멤버 함수는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.

##  <a name="getrect"></a>  CRichEditCtrl::GetRect

이 `CRichEditCtrl` 개체의 서식 지정 사각형을 검색 합니다.

```
void GetRect(LPRECT lpRect) const;
```

### <a name="parameters"></a>매개 변수

*lpRect*<br/>
이 `CRichEditCtrl` 개체 의 서식 지정 사각형을 받는 [사각형](/windows/win32/api/windef/ns-windef-rect)에 대한 [crect](../../atl-mfc-shared/reference/crect-class.md) 또는 포인터입니다.

### <a name="remarks"></a>설명

서식 지정 사각형은 텍스트의 경계 사각형입니다. 이 값은 `CRichEditCtrl` 개체의 크기와는 독립적입니다.

자세한 내용은 Windows SDK에서 [EM_GETRECT](/windows/win32/Controls/em-getrect) 을 참조 하세요.

### <a name="example"></a>예제

  대만 [텍스트](#limittext)는 예제를 참조 하세요.

##  <a name="getredoname"></a>  CRichEditCtrl::GetRedoName

Redo queue에서 사용 가능한 다음 작업의 형식 (있는 경우)을 검색 합니다.

```
UNDONAMEID GetRedoName() const;
```

### <a name="return-value"></a>반환 값

성공 하 `GetRedoName` 는 경우 컨트롤의 redo queue에서 다음 작업의 형식을 나타내는 [UNDONAMEID](/windows/win32/api/richedit/ne-richedit-undonameid) 열거형 형식을 반환 합니다. Redo queue가 비어 있거나 큐의 다시 실행 작업이 알 수 없는 형식인 경우는 0을 `GetRedoName` 반환 합니다.

### <a name="remarks"></a>설명

실행 취소 하거나 다시 실행할 수 있는 동작의 유형에는 입력, 삭제, 끌어서 놓기, 잘라내기 및 붙여넣기 작업이 포함 됩니다. 이 정보는 redoable 작업의 드롭다운 목록 상자와 같은 실행 취소 및 다시 실행 작업을 위한 확장 사용자 인터페이스를 제공 하는 응용 프로그램에 유용할 수 있습니다.

##  <a name="getsel"></a>  CRichEditCtrl::GetSel

이 `CRichEditCtrl` 개체에서 현재 선택 영역에 대 한 범위를 검색 합니다.

```
void GetSel(CHARRANGE& cr) const;

void GetSel(
    long& nStartChar,
    long& nEndChar) const;
```

### <a name="parameters"></a>매개 변수

*cr*<br/>
현재 선택 영역의 범위를 받는 [Charrange](/windows/win32/api/richedit/ns-richedit-charrange) 구조체에 대 한 참조입니다.

*nStartChar*<br/>
현재 선택 영역에 있는 첫 번째 문자의 0부터 시작 하는 인덱스입니다.

*nEndChar*<br/>
현재 선택 영역에 있는 마지막 문자의 0부터 시작 하는 인덱스입니다.

### <a name="remarks"></a>설명

이 함수의 두 가지 형태는 선택 영역에 대 한 범위를 가져오는 또 다른 방법을 제공 합니다. 이러한 폼에 대 한 간략 한 설명은 다음과 같습니다.

- **Getsel (** `cr` **)** 이 `CHARRANGE` 폼은`cpMax`및멤버가 포함 된 구조체를 사용 하 여 범위를 반환 합니다. `cpMin`

- **Getsel (** `nStartChar` **,** `nEndChar` **)** 이 양식은 *nstartchar* 및 *nendchar*매개 변수의 범위를 반환 합니다.

시작 (`cpMin` 또는 *nstartchar*)이 0이 고 end (`cpMax` 또는 *nendchar*)가-1 이면 선택 항목에 모든 항목이 포함 됩니다.

자세한 내용은 [EM_EXGETSEL](/windows/win32/Controls/em-exgetsel) Message And [charrange](/windows/win32/api/richedit/ns-richedit-charrange) structure in the Windows SDK를 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#15](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_15.cpp)]

##  <a name="getselectioncharformat"></a>  CRichEditCtrl::GetSelectionCharFormat

현재 선택 영역의 문자 서식 특성을 가져옵니다.

```
DWORD GetSelectionCharFormat(CHARFORMAT& cf) const;  DWORD GetSelectionCharFormat(CHARFORMAT2& cf) const;
```

### <a name="parameters"></a>매개 변수

*cf*<br/>
첫 번째 버전에서 현재 선택의 문자 서식 특성을 받는 [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-_charformat) 구조체에 대 한 포인터입니다.

두 번째 버전에서 [CHARFORMAT2](/windows/win32/api/richedit/ns-richedit-charformat2w) 구조체에 대 한 포인터로, 현재 선택의 문자 서식 특성을 수신 하는 `CHARFORMAT` 구조에 대 한 Rich Edit 2.0 extension입니다.

### <a name="return-value"></a>반환 값

`dwMask` *Cf*의 데이터 멤버입니다. 현재 선택 영역 전체에서 일치 하는 문자 서식 특성을 지정 합니다.

### <a name="remarks"></a>설명

*Cf* 매개 변수는 현재 선택 영역에 있는 첫 번째 문자의 특성을 받습니다. 반환 값은 선택 영역 전체에서 일치 하는 특성을 지정 합니다.

자세한 내용은 Windows SDK [EM_GETCHARFORMAT](/windows/win32/Controls/em-getcharformat) 메시지 및 `CHARFORMAT` 및 `CHARFORMAT2` 구조체를 참조 하십시오.

### <a name="example"></a>예제

  [SetSelectionCharFormat](#setselectioncharformat)의 예제를 참조 하세요.

##  <a name="getselectiontype"></a>  CRichEditCtrl::GetSelectionType

이 `CRichEditCtrl` 개체의 선택 유형을 결정 합니다.

```
WORD GetSelectionType() const;
```

### <a name="return-value"></a>반환 값

현재 선택 영역의 내용을 나타내는 플래그입니다. 다음 플래그의 조합입니다.

- SEL_EMPTY은 현재 선택 영역이 없음을 나타냅니다.

- SEL_TEXT는 현재 선택 영역에 텍스트가 포함 되어 있음을 나타냅니다.

- SEL_OBJECT는 현재 선택 영역에 하나 이상의 OLE 항목이 포함 되어 있음을 나타냅니다.

- SEL_MULTICHAR는 현재 선택에 텍스트 문자가 두 개 이상 포함 되어 있음을 나타냅니다.

- SEL_MULTIOBJECT는 현재 선택 영역에 둘 이상의 OLE 개체가 포함 되어 있음을 나타냅니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_SELECTIONTYPE](/windows/win32/Controls/em-selectiontype) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#16](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_16.cpp)]

##  <a name="getseltext"></a>  CRichEditCtrl::GetSelText

이 `CRichEditCtrl` 개체의 현재 선택 영역에서 텍스트를 검색 합니다.

```
long GetSelText(LPSTR lpBuf) const;  CString GetSelText() const;
```

### <a name="parameters"></a>매개 변수

*lpBuf*<br/>
현재 선택 영역에 있는 텍스트를 받을 버퍼에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

다음 형식에 따라 달라 집니다.

- **Getseltext (** `lpBuf` **)** null 종료를 포함 하지 않고 *lpbuf*로 복사 되는 문자 수입니다.

- **Getseltext ()** 현재 선택 영역을 포함 하는 문자열입니다.

### <a name="remarks"></a>설명

첫 번째 폼인 **getseltext (** `lpBuf` **)** 를 사용 하는 경우 버퍼가 수신 하는 텍스트에 대해 버퍼가 충분히 큰지 확인 해야 합니다. [Getsel](#getsel) 을 호출 하 여 현재 선택 영역에 있는 문자 수를 확인 합니다.

자세한 내용은 Windows SDK에서 [EM_GETSELTEXT](/windows/win32/Controls/em-getseltext) 을 참조 하세요.

### <a name="example"></a>예제

  [CRichEditCtrl:: GetSelectionType](#getselectiontype)의 예제를 참조 하세요.

##  <a name="gettextlength"></a>  CRichEditCtrl::GetTextLength

이 `CRichEditCtrl` 개체에서 종료 null 문자를 포함 하지 않는 텍스트의 길이 (문자 수)를 검색 합니다.

```
long GetTextLength() const;
```

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체에 있는 텍스트의 길이입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [WM_GETTEXTLENGTH](/windows/win32/winmsg/wm-gettextlength) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#17](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_17.cpp)]

##  <a name="gettextlengthex"></a>  CRichEditCtrl::GetTextLengthEx

Rich edit 컨트롤의 텍스트 길이를 계산 합니다.

```
long GetTextLengthEx(
    DWORD dwFlags,
    UINT uCodePage = -1) const;
```

### <a name="parameters"></a>매개 변수

*dwFlags*<br/>
텍스트 길이를 결정 하는 데 사용할 메서드를 지정 하는 값입니다. 이 멤버는 Windows SDK에 설명 된 [GETTEXTLENGTHEX](/windows/win32/api/richedit/ns-richedit-gettextlengthex) 의 flags 멤버에 나열 된 값 중 하나 이상이 될 수 있습니다.

*uCodePage*<br/>
변환에 대 한 코드 페이지 (ANSI 코드 페이지의 경우 CP_ACP, 유니코드의 경우 1200)

### <a name="return-value"></a>반환 값

편집 컨트롤의 문자 또는 바이트 수입니다. *DwFlags*에 호환 되지 않는 플래그가 설정 된 경우이 멤버 함수는 E_INVALIDARG를 반환 합니다.

### <a name="remarks"></a>설명

`GetTextLengthEx`텍스트의 길이를 결정 하는 추가 방법을 제공 합니다. Rich Edit 2.0 기능을 지원 합니다. 자세한 내용은 Windows SDKfor의 [Rich Edit 컨트롤](/windows/win32/Controls/about-rich-edit-controls) 정보를 참조 하세요.

##  <a name="gettextmode"></a>  CRichEditCtrl::GetTextMode

Rich edit 컨트롤의 현재 텍스트 모드와 실행 취소 수준을 검색 합니다.

```
UINT GetTextMode() const;
```

### <a name="return-value"></a>반환 값

Windows SDK 설명 된 대로 [TEXTMODE](/windows/win32/api/richedit/ne-richedit-textmode) 열거형 형식의 비트 플래그 집합입니다. 플래그는 컨트롤의 현재 텍스트 모드와 실행 취소 수준을 표시 합니다.

##  <a name="gettextrange"></a>  CRichEditCtrl::GetTextRange

지정 된 문자 범위를 가져옵니다.

```
int GetTextRange(
    int nFirst,
    int nLast,
    CString& refString) const;
```

### <a name="parameters"></a>매개 변수

*nFirst*<br/>
범위의 첫 번째 문자 바로 앞에 있는 문자 위치 인덱스입니다.

*nLast*<br/>
범위에서 마지막 문자 바로 다음에 오는 문자 위치입니다.

*refString*<br/>
텍스트를 받을 [CString](../../atl-mfc-shared/reference/cstringt-class.md) 개체에 대 한 참조입니다.

### <a name="return-value"></a>반환 값

종결 null 문자를 포함 하지 않고 복사 된 문자 수입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_GETTEXTRANGE](/windows/win32/Controls/em-gettextrange) 을 참조 하세요.

`GetTextRange`는 Rich Edit 2.0 기능을 지원 합니다. 자세한 내용은 Windows SDKfor의 [Rich Edit 컨트롤](/windows/win32/Controls/about-rich-edit-controls) 정보를 참조 하세요.

##  <a name="getundoname"></a>  CRichEditCtrl::GetUndoName

실행 취소 큐에서 사용 가능한 다음 작업의 형식 (있는 경우)을 검색 합니다.

```
UNDONAMEID GetUndoName() const;
```

### <a name="return-value"></a>반환 값

실행 취소 작업이 컨트롤의 실행 취소 큐 `GetUndoName` 에 있는 경우는 큐에서 다음 작업의 유형을 나타내는 [UNDONAMEID](/windows/win32/api/richedit/ne-richedit-undonameid) 열거 형식을 반환 합니다. 실행 취소 큐가 비어 있거나 큐의 실행 취소 작업이 알 수 없는 형식인 경우는 0을 `GetUndoName` 반환 합니다.

### <a name="remarks"></a>설명

실행 취소 하거나 다시 실행할 수 있는 동작의 유형에는 입력, 삭제, 끌어서 놓기, 잘라내기 및 붙여넣기 작업이 포함 됩니다. 이 정보는 실행 취소할 수 있는 작업의 드롭다운 목록 상자와 같이 실행 취소 및 다시 실행 작업에 대 한 확장 된 사용자 인터페이스를 제공 하는 응용 프로그램에 유용할 수 있습니다.

##  <a name="getwordwrapmode"></a>  CRichEditCtrl::GetWordWrapMode

Rich edit 컨트롤의 현재 단어 줄 바꿈 및 단어 분리 옵션을 검색 합니다.

```
UINT GetWordWrapMode() const;
```

### <a name="return-value"></a>반환 값

현재 단어 줄 바꿈 및 단어 분리 옵션입니다. 이러한 옵션은 Windows SDK의 [EM_SETWORDWRAPMODE](/windows/win32/Controls/em-setwordwrapmode) 에 설명 되어 있습니다.

### <a name="remarks"></a>설명

이 구성원 함수는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.

##  <a name="hideselection"></a>  CRichEditCtrl::HideSelection

선택 영역의 표시 여부를 변경 합니다.

```
void HideSelection(
    BOOL bHide,
    BOOL bPerm);
```

### <a name="parameters"></a>매개 변수

*bHide*<br/>
선택 영역을 표시 하거나 숨길지 여부를 나타냅니다. 선택 영역을 숨기려면 TRUE로 설정 합니다.

*bPerm*<br/>
선택 항목에 대 한 표시 유형 변경이 영구적 이어야 하는지 여부를 나타냅니다.

### <a name="remarks"></a>설명

*Bperm* 이 TRUE 이면이 `CRichEditCtrl` 개체에 대 한 ECO_NOHIDESEL 옵션을 변경 합니다. 이 옵션에 대 한 간략 한 설명은 [SetOptions](#setoptions)를 참조 하세요. 이 함수를 사용 하 여이 `CRichEditCtrl` 개체에 대 한 모든 옵션을 설정할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_HIDESELECTION](/windows/win32/Controls/em-hideselection) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#18](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_18.cpp)]

##  <a name="limittext"></a>  CRichEditCtrl::LimitText

사용자가 편집 컨트롤에 입력할 수 있는 텍스트의 길이를 제한 합니다.

```
void LimitText(long nChars = 0);
```

### <a name="parameters"></a>매개 변수

*nChars*<br/>
사용자가 입력할 수 있는 텍스트의 길이 (바이트)를 지정 합니다. 이 매개 변수 값이 0 (기본값) 이면 텍스트 길이가 64K 바이트로 설정 됩니다.

### <a name="remarks"></a>설명

텍스트 제한을 변경 하면 사용자가 입력할 수 있는 텍스트만 제한 됩니다. 편집 컨트롤에 이미 있는 텍스트에는 영향을 주지 않으며의 `CWnd` [setwindowtext](../../mfc/reference/cwnd-class.md#setwindowtext) 멤버 함수를 통해 편집 컨트롤에 복사 되는 텍스트의 길이에도 영향을 주지 않습니다. 응용 프로그램에서 `SetWindowText` 함수를 사용 하 여에 대 `LimitText`한 호출에 지정 된 것 보다 더 많은 텍스트를 편집 컨트롤에 추가 하는 경우 사용자는 편집 컨트롤 내에서 텍스트를 삭제할 수 있습니다. 그러나 현재 선택 영역을 삭제 하면 텍스트가 텍스트 제한 아래에 포함 되는 경우를 제외 하 고는 텍스트 제한으로 인해 사용자가 기존 텍스트를 새 텍스트로 바꿀 수 없습니다.

> [!NOTE]
>  텍스트 제한의 경우 각 OLE 항목은 단일 문자로 계산 됩니다.

자세한 내용은 Windows SDK에서 [EM_EXLIMITTEXT](/windows/win32/Controls/em-exlimittext) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#19](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_19.cpp)]

##  <a name="linefromchar"></a>  CRichEditCtrl::LineFromChar

지정 된 문자 인덱스를 포함 하는 줄의 줄 번호를 검색 합니다.

```
long LineFromChar(long nIndex) const;
```

### <a name="parameters"></a>매개 변수

*nIndex*<br/>
편집 컨트롤의 텍스트에 있는 원하는 문자에 대 한 0부터 시작 하는 인덱스 값을 포함 하거나-1을 포함 합니다. *Nindex* 가-1 인 경우 현재 줄, 즉 캐럿이 포함 된 줄을 지정 합니다.

### <a name="return-value"></a>반환 값

*Nindex*로 지정 된 문자 인덱스를 포함 하는 줄의 줄 번호 (0부터 시작)입니다. *Nindex* 가-1 이면 선택의 첫 문자가 포함 된 줄 번호가 반환 됩니다. 선택 항목이 없으면 현재 줄 번호가 반환 됩니다.

### <a name="remarks"></a>설명

문자 인덱스는 rich edit 컨트롤의 시작 부분에 있는 문자 수입니다. 문자 수를 계산 하는 경우 OLE 항목은 단일 문자로 계산 됩니다.

자세한 내용은 Windows SDK에서 [EM_EXLINEFROMCHAR](/windows/win32/Controls/em-exlinefromchar) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#20](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_20.cpp)]

##  <a name="lineindex"></a>  CRichEditCtrl::LineIndex

이 `CRichEditCtrl` 개체에서 줄의 문자 인덱스를 검색 합니다.

```
int LineIndex(int nLine = -1) const;
```

### <a name="parameters"></a>매개 변수

*nLine*<br/>
편집 컨트롤의 텍스트에 있는 원하는 줄의 인덱스 값을 포함 하거나-1을 포함 합니다. *Nline* 이-1 이면 캐럿을 포함 하는 줄 인 현재 줄을 지정 합니다.

### <a name="return-value"></a>반환 값

*Nline* 에 지정 된 줄의 문자 인덱스 이거나, 지정 된 줄 번호가 편집 컨트롤의 줄 수 보다 큰 경우-1입니다.

### <a name="remarks"></a>설명

문자 인덱스는 서식 있는 편집 컨트롤의 시작 부분부터 지정 된 줄 까지의 문자 수입니다.

자세한 내용은 Windows SDK에서 [EM_LINEINDEX](/windows/win32/controls/em-lineindex) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#21](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_21.cpp)]

##  <a name="linelength"></a>  CRichEditCtrl::LineLength

Rich edit 컨트롤에서 줄의 길이를 검색 합니다.

```
int LineLength(int nLine = -1) const;
```

### <a name="parameters"></a>매개 변수

*nLine*<br/>
해당 길이를 검색할 줄의 문자 인덱스를 지정 합니다. 이 매개 변수가-1 이면 줄 내에서 선택한 텍스트의 길이를 포함 하지 않고 현재 줄의 길이 (캐럿을 포함 하는 줄)가 반환 됩니다. 한 `LineLength` 줄 편집 컨트롤에 대해를 호출 하는 경우이 매개 변수는 무시 됩니다.

### <a name="return-value"></a>반환 값

여러 `LineLength` 줄 편집 컨트롤에 대해를 호출 하는 경우 반환 값은 *nline*으로 지정 된 줄의 길이 (tchar.h)입니다.  줄 끝에 캐리지 리턴 문자를 포함 하지 않습니다. 한 `LineLength` 줄 편집 컨트롤에 대해를 호출 하는 경우 반환 값은 편집 컨트롤에 있는 텍스트의 길이 (tchar.h)입니다. NLine이 컨트롤의 문자 수보다 크면 반환 값은 0입니다.

### <a name="remarks"></a>설명

[Lineindex](#lineindex) 멤버 함수를 사용 하 여이 `CRichEditCtrl` 개체 내의 지정 된 줄 번호에 대 한 문자 인덱스를 검색 합니다.

자세한 내용은 Windows SDK에서 [EM_LINELENGTH](/windows/win32/Controls/em-linelength) 을 참조 하세요.

### <a name="example"></a>예제

  [Lineindex](#lineindex)의 예제를 참조 하세요.

##  <a name="linescroll"></a>  CRichEditCtrl::LineScroll

여러 줄 편집 컨트롤의 텍스트를 스크롤합니다.

```
void LineScroll(
    int nLines,
    int nChars = 0);
```

### <a name="parameters"></a>매개 변수

*nLines*<br/>
세로로 스크롤할 줄 수를 지정 합니다.

*nChars*<br/>
가로로 스크롤할 문자 위치 수를 지정 합니다. Rich edit 컨트롤에 ES_RIGHT 또는 ES_CENTER 스타일이 있는 경우이 값은 무시 됩니다. [편집 스타일](../../mfc/reference/styles-used-by-mfc.md#edit-styles) 은 [만들기](#create)에서 지정 됩니다.

### <a name="remarks"></a>설명

편집 컨트롤은 편집 컨트롤에서 텍스트의 마지막 줄을 벗어나 세로로 스크롤되지 않습니다. 현재 줄과 Nlines에 지정 된 줄 수가 모두 편집 컨트롤의 총 줄 수를 초과 하는 경우 편집 컨트롤의 마지막 줄이 편집 컨트롤 창의 맨 위로 스크롤 되도록 값이 조정 됩니다.

`LineScroll`줄의 마지막 문자를 지나서 가로로 스크롤 하는 데 사용할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_LINESCROLL](/windows/win32/Controls/em-linescroll) 을 참조 하세요.

### <a name="example"></a>예제

  [GetFirstVisibleLine](#getfirstvisibleline)의 예제를 참조 하세요.

##  <a name="paste"></a>  CRichEditCtrl::Paste

클립보드 `CRichEditCtrl` 의 데이터를 삽입 지점의에 캐럿 위치에 삽입 합니다.

```
void Paste();
```

### <a name="remarks"></a>설명

데이터가 인식할 수 있는 형식의 데이터를 포함 하는 경우에만 데이터가 삽입 됩니다.

자세한 내용은 Windows SDK에서 [WM_PASTE](/windows/win32/dataxchg/wm-paste) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#22](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_22.cpp)]

##  <a name="pastespecial"></a>  CRichEditCtrl::PasteSpecial

특정 클립보드 형식의 데이터를이 `CRichEditCtrl` 개체에 붙여넣습니다.

```
void PasteSpecial(
    UINT nClipFormat,
    DWORD dvAspect = 0,
    HMETAFILE hMF = 0);
```

### <a name="parameters"></a>매개 변수

*nClipFormat*<br/>
이 `CRichEditCtrl` 개체에 붙여넣을 클립보드 형식입니다.

*dvAspect*<br/>
클립보드에서 검색할 데이터의 장치 측면입니다.

*hMF*<br/>
붙여 넣을 개체의 아이콘 뷰를 포함 하는 메타 파일에 대 한 핸들입니다.

### <a name="remarks"></a>설명

새 자료는 삽입 지점에서 캐럿의 위치에 삽입 됩니다.

자세한 내용은 Windows SDK에서 [EM_PASTESPECIAL](/windows/win32/Controls/em-pastespecial) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#23](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_23.cpp)]

##  <a name="posfromchar"></a>  CRichEditCtrl::PosFromChar

편집 컨트롤에서 지정 된 문자의 클라이언트 영역 좌표를 검색 합니다.

```
CPoint PosFromChar(UINT nChar) const;
```

### <a name="parameters"></a>매개 변수

*nChar*<br/>
문자의 0부터 시작 하는 인덱스입니다.

### <a name="return-value"></a>반환 값

문자 (x, y)의 위치입니다. 한 줄 편집 컨트롤의 경우 y 좌표는 항상 0입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_POSFROMCHAR](/windows/win32/Controls/em-posfromchar) 을 참조 하세요.

##  <a name="redo"></a>  CRichEditCtrl::Redo

컨트롤의 redo queue에서 다음 작업을 다시 실행 합니다.

```
BOOL Redo();
```

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

자세한 내용은 Windows SDK에서 [EM_REDO](/windows/win32/Controls/em-redo) 을 참조 하세요.

##  <a name="replacesel"></a>  CRichEditCtrl::ReplaceSel

이 `CRichEditCtrl` 개체의 현재 선택 영역을 지정 된 텍스트로 바꿉니다.

```
void ReplaceSel(
    LPCTSTR lpszNewText,
    BOOL bCanUndo = FALSE);
```

### <a name="parameters"></a>매개 변수

*lpszNewText*<br/>
대체 텍스트를 포함 하는 null로 끝나는 문자열에 대 한 포인터입니다.

*bCanUndo*<br/>
이 함수를 실행 취소할 수 있도록 지정 하려면이 매개 변수의 값을 TRUE로 설정 합니다. 기본값은 FALSE입니다.

### <a name="remarks"></a>설명

이 `CRichEditCtrl` 개체의 모든 텍스트를 바꾸려면 [CWnd:: setwindowtext](../../mfc/reference/cwnd-class.md#setwindowtext)를 사용 합니다.

현재 선택 항목이 없으면 삽입 지점, 즉 현재 캐럿 위치에 대체 텍스트가 삽입 됩니다.

이 함수는 기존 문자 서식으로 삽입 된 텍스트의 서식을 지정 합니다. 를 호출 `SetSel` `ReplaceSel`하기 전에 (0,-1)을 호출 하 여 전체 텍스트 범위를 바꿀 때 새로 삽입 된 텍스트에 의해 상속 되는 이전 단락의 서식을 유지 하는 단락 끝 문자는 있습니다.

자세한 내용은 Windows SDK에서 [EM_REPLACESEL](/windows/win32/Controls/em-replacesel) 을 참조 하세요.

### <a name="example"></a>예제

  [Lineindex](#lineindex)의 예제를 참조 하세요.

##  <a name="requestresize"></a>  CRichEditCtrl::RequestResize

이 `CRichEditCtrl` 개체가 부모 창에 EN_REQUESTRESIZE 알림 메시지를 보내도록 합니다.

```
void RequestResize();
```

### <a name="remarks"></a>설명

이 함수는 무제한 `CRichEditCtrl` 개체에 대 한 [CWnd:: onsize](../../mfc/reference/cwnd-class.md#onsize) 처리 중에 유용 합니다.

자세한 내용은 Windows SDK의 [Rich Edit 컨트롤에 대 한](/windows/win32/Controls/about-rich-edit-controls) [EM_REQUESTRESIZE](/windows/win32/Controls/em-requestresize) 메시지 및 **무제한 rich edit controls** 섹션을 참조 하세요.

##  <a name="setautourldetect"></a>  CRichEditCtrl::SetAutoURLDetect

URL을 자동으로 검색 하도록 rich edit 컨트롤을 설정 합니다.

```
BOOL SetAutoURLDetect(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>매개 변수

*bEnable*<br/>
컨트롤이 URL을 자동으로 검색 하도록 설정 되었는지 여부를 지정 합니다. TRUE 이면 활성화 됩니다. FALSE 이면 사용 하지 않도록 설정 됩니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 고, 그렇지 않으면 0입니다. 예를 들어 메모리 부족으로 인해 메시지가 실패할 수 있습니다.

### <a name="remarks"></a>설명

사용 하도록 설정 되 면 rich edit 컨트롤에서 텍스트를 검색 하 여 표준 URL 형식과 일치 하는지 확인 합니다. 이러한 URL 형식 목록은 Windows SDK의 [EM_AUTOURLDETECT](/windows/win32/Controls/em-autourldetect) 를 참조 하세요.

> [!NOTE]
>  편집 컨트롤이 url `SetAutoURLDetect` 이외의 텍스트에 대해 CFE_LINK 효과를 사용 하는 경우 TRUE로 설정 하지 마십시오. `SetAutoURLDetect`Url에 대해이 효과를 사용 하도록 설정 하 고 다른 모든 텍스트에 대해 사용 하지 않도록 설정 합니다. CFE_LINK 효과에 대 한 자세한 내용은 [EN_LINK](/windows/win32/Controls/en-link) 를 참조 하세요.

##  <a name="setbackgroundcolor"></a>  CRichEditCtrl::SetBackgroundColor

이 `CRichEditCtrl` 개체의 배경색을 설정 합니다.

```
COLORREF SetBackgroundColor(
    BOOL bSysColor,
    COLORREF cr);
```

### <a name="parameters"></a>매개 변수

*bSysColor*<br/>
배경색을 시스템 값으로 설정 해야 하는지 여부를 나타냅니다. 이 값이 TRUE 이면 *cr* 이 무시 됩니다.

*cr*<br/>
요청한 배경색입니다. *Bsyscolor* 가 FALSE 인 경우에만 사용 됩니다.

### <a name="return-value"></a>반환 값

이 `CRichEditCtrl` 개체의 이전 배경색입니다.

### <a name="remarks"></a>설명

배경색은 시스템 값 또는 지정 된 [Colorref](/windows/win32/gdi/colorref) 값으로 설정할 수 있습니다.

자세한 내용은 Windows SDK [EM_SETBKGNDCOLOR](/windows/win32/Controls/em-setbkgndcolor) Message And [colorref](/windows/win32/gdi/colorref) structure를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#24](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_24.cpp)]

##  <a name="setdefaultcharformat"></a>  CRichEditCtrl::SetDefaultCharFormat

이 `CRichEditCtrl` 개체의 새 텍스트에 대 한 문자 서식 특성을 설정 합니다.

```
BOOL SetDefaultCharFormat(CHARFORMAT& cf);
BOOL SetDefaultCharFormat(CHARFORMAT2& cf);
```

### <a name="parameters"></a>매개 변수

*cf*<br/>
첫 번째 버전에서 새 기본 문자 서식 특성을 포함 하는 [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-_charformat) 구조체에 대 한 포인터입니다.

두 번째 버전에서 기본 문자 서식 특성을 포함 하는 `CHARFORMAT` 구조에 대 한 Rich Edit 2.0 extension 인 [CHARFORMAT2](/windows/win32/api/richedit/ns-richedit-charformat2w) 구조체에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 함수는 `dwMask` *cf* 의 멤버로 지정 된 특성만 변경 합니다.

자세한 내용은 Windows SDK [EM_SETCHARFORMAT](/windows/win32/Controls/em-setcharformat) 메시지 및 `CHARFORMAT` 및 `CHARFORMAT2` 구조체를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#25](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_25.cpp)]

##  <a name="seteventmask"></a>  CRichEditCtrl::SetEventMask

이 `CRichEditCtrl` 개체에 대 한 이벤트 마스크를 설정 합니다.

```
DWORD SetEventMask(DWORD dwEventMask);
```

### <a name="parameters"></a>매개 변수

*dwEventMask*<br/>
이 `CRichEditCtrl` 개체에 대 한 새 이벤트 마스크입니다.

### <a name="return-value"></a>반환 값

이전 이벤트 마스크입니다.

### <a name="remarks"></a>설명

이벤트 마스크는 `CRichEditCtrl` 개체가 부모 창에 보내는 알림 메시지를 지정 합니다.

자세한 내용은 Windows SDK에서 [EM_SETEVENTMASK](/windows/win32/Controls/em-seteventmask) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#26](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_26.cpp)]

##  <a name="setmodify"></a>  CRichEditCtrl::SetModify

편집 컨트롤에 대해 수정 된 플래그를 설정 하거나 취소 합니다.

```
void SetModify(BOOL bModified = TRUE);
```

### <a name="parameters"></a>매개 변수

*bModified*<br/>
TRUE 값은 텍스트가 수정 되었음을 나타내고, FALSE 값은 수정 되지 않았음을 나타냅니다. 기본적으로 수정 된 플래그는 설정 되어 있습니다.

### <a name="remarks"></a>설명

수정 된 플래그는 편집 컨트롤 내의 텍스트가 수정 되었는지 여부를 나타냅니다. 사용자가 텍스트를 변경할 때마다 자동으로 설정 됩니다. [Getmodify](#getmodify) 멤버 함수를 사용 하 여 해당 값을 검색할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_SETMODIFY](/windows/win32/Controls/em-setmodify) 을 참조 하세요.

### <a name="example"></a>예제

  [Getmodify](#getmodify)의 예제를 참조 하세요.

##  <a name="setolecallback"></a>  CRichEditCtrl::SetOLECallback

OLE 관련 `CRichEditCtrl` 리소스 및 `IRichEditOleCallback` 정보에 액세스 하는 데 사용할 개체를이 개체에 제공 합니다.

```
BOOL SetOLECallback(IRichEditOleCallback* pCallback);
```

### <a name="parameters"></a>매개 변수

*pCallback*<br/>
이`CRichEditCtrl` 개체가 OLE 관련 리소스 및 정보를 가져오는 데 사용 하는 [IRichEditOleCallback](/windows/win32/api/richole/nn-richole-iricheditolecallback) 개체에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 `CRichEditCtrl` 개체는 [IUnknown:: AddRef](/windows/win32/api/unknwn/nf-unknwn-iunknown-addref) 를 호출 하 여 *pcallback*에 지정 된 COM 개체의 사용 횟수를 늘립니다.

자세한 내용은 Windows SDK [EM_SETOLECALLBACK](/windows/win32/Controls/em-setolecallback) Message and [IRichEditOleCallback](/windows/win32/api/richole/nn-richole-iricheditolecallback) interface 항목을 참조 하세요.

##  <a name="setoptions"></a>  CRichEditCtrl::SetOptions

이 `CRichEditCtrl` 개체에 대 한 옵션을 설정 합니다.

```
void SetOptions(
    WORD wOp,
    DWORD dwFlags);
```

### <a name="parameters"></a>매개 변수

*wOp*<br/>
작업 유형을 나타냅니다. 다음 값 중 하나입니다.

- ECOOP_SET 옵션을 *dwFlags*로 지정 된 옵션으로 설정 합니다.

- ECOOP_OR 현재 옵션을 *dwFlags*에 지정 된 옵션과 결합 합니다.

- ECOOP_AND는 *dwFlags*에도 지정 된 현재 옵션만 보존 합니다.

- ECOOP_XOR는 *dwFlags*로 지정 된 옵션을 사용 하 여 논리적으로 배타적으로 또는 현재 옵션을 사용 합니다.

*dwFlags*<br/>
Rich edit 옵션입니다. 플래그 값은 설명 섹션에 나열 되어 있습니다.

### <a name="remarks"></a>설명

옵션은 다음 값을 조합 하 여 사용할 수 있습니다.

- ECO_AUTOWORDSELECTION를 두 번 클릭 하면 자동으로 선택 됩니다.

- ECO_AUTOVSCROLL 사용자가 줄의 끝에 문자를 입력 하면 자동으로 텍스트를 오른쪽으로 10 자로 스크롤합니다. 사용자가 ENTER 키를 누를 때 컨트롤은 모든 텍스트를 위치 0으로 다시 스크롤합니다.

- ECO_AUTOHSCROLL 사용자가 마지막 줄에서 ENTER 키를 누르면 자동으로 한 페이지씩 텍스트를 스크롤합니다.

- ECO_NOHIDESEL는 편집 컨트롤의 기본 동작을 부정 합니다. 기본 동작은 컨트롤이 입력 포커스를 잃었을 때 선택을 숨기고 컨트롤이 입력 포커스를 받을 때 선택 항목을 표시 합니다. ECO_NOHIDESEL를 지정 하는 경우 컨트롤에 포커스가 없더라도 선택한 텍스트가 반전 됩니다.

- ECO_READONLY를 사용 하면 사용자가 편집 컨트롤의 텍스트를 입력 하거나 편집할 수 없습니다.

- ECO_WANTRETURN는 사용자가 ENTER 키를 누를 때 ENTER 키를 누를 때 캐리지 리턴을 삽입 하도록 지정 하 고 대화 상자에서 여러 줄 서식 있는 편집 컨트롤에 텍스트를 입력 합니다. 이 스타일을 지정 하지 않으면 ENTER 키를 누르면 rich edit 컨트롤의 부모 창에 명령이 전송 됩니다 .이 창에는 부모 창의 기본 단추 (예: 대화 상자의 확인 단추)를 클릭 하는 것과 유사 합니다. 이 스타일은 한 줄 편집 컨트롤에는 영향을 주지 않습니다.

- ECO_SAVESEL 컨트롤이 포커스를 잃었을 때 선택 항목을 유지 합니다. 기본적으로 컨트롤의 전체 내용이 포커스를 다시 얻으면 선택 됩니다.

- ECO_VERTICAL는 텍스트와 개체를 세로 방향으로 그립니다. 아시아 언어에만 사용할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_SETOPTIONS](/windows/win32/Controls/em-setoptions) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#27](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_27.cpp)]

##  <a name="setparaformat"></a>  CRichEditCtrl::SetParaFormat

이 `CRichEditCtrl` 개체의 현재 선택 영역에 대 한 단락 서식 특성을 설정 합니다.

```
BOOL SetParaFormat(PARAFORMAT& pf);
BOOL SetParaFormat(PARAFORMAT2& pf);
```

### <a name="parameters"></a>매개 변수

*pf*<br/>
첫 번째 버전에서 새 기본 단락 서식 특성을 포함 하는 [PARAFORMAT](/windows/win32/api/richedit/ns-richedit-paraformat) 구조체에 대 한 포인터입니다.

두 번째 버전에서 기본 문자 서식 특성을 보유 한 `PARAFORMAT` 구조에 대 한 Rich Edit 2.0 extension 인 [PARAFORMAT2](/windows/win32/api/richedit/ns-richedit-paraformat2) 구조체에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 함수는 `dwMask` *pf* 의 멤버로 지정 된 특성만 변경 합니다.

자세한 내용은 Windows SDK [EM_SETPARAFORMAT](/windows/win32/Controls/em-setparaformat) 메시지 및 `PARAFORMAT` 및 `PARAFORMAT2` 구조체를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#28](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_28.cpp)]

##  <a name="setpunctuation"></a>  CRichEditCtrl::SetPunctuation

Rich edit 컨트롤에서 문장 부호를 설정 합니다.

```
BOOL SetPunctuation(
    UINT fType,
    PUNCTUATION* lpPunc);
```

### <a name="parameters"></a>매개 변수

*fType*<br/>
문장 부호 플래그입니다. 가능한 값 목록은 Windows SDK의 [EM_SETPUNCTUATION](/windows/win32/Controls/em-setpunctuation) 에 대 한 *fType* 매개 변수를 참조 하세요.

*lpPunc*<br/>
Windows SDK에 설명 된 [문장 부호](/windows/win32/api/richedit/ns-richedit-punctuation) 구조에 대 한 포인터입니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값이 고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 구성원 함수는 운영 체제의 아시아 언어 버전에만 사용할 수 있습니다.

##  <a name="setreadonly"></a>  CRichEditCtrl::SetReadOnly

이 `CRichEditCtrl` 개체에 대 한 ECO_READONLY 옵션을 변경 합니다.

```
BOOL SetReadOnly(BOOL bReadOnly = TRUE);
```

### <a name="parameters"></a>매개 변수

*bReadOnly*<br/>
이 `CRichEditCtrl` 개체를 읽어야 하는지 여부를 나타냅니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 옵션에 대 한 간략 한 설명은 [SetOptions](#setoptions)를 참조 하세요. 이 함수를 사용 하 여이 `CRichEditCtrl` 개체에 대 한 모든 옵션을 설정할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_SETREADONLY](/windows/win32/Controls/em-setreadonly) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#29](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_29.cpp)]

##  <a name="setrect"></a>  CRichEditCtrl::SetRect

이 `CRichEditCtrl` 개체의 서식 지정 사각형을 설정 합니다.

```
void SetRect(LPCRECT lpRect);
```

### <a name="parameters"></a>매개 변수

*lpRect*<br/>
서식 지정 사각형의 새 경계 를 나타내는 [사각형](/windows/win32/api/windef/ns-windef-rect)에 대한 [crect](../../atl-mfc-shared/reference/crect-class.md) 또는 포인터입니다.

### <a name="remarks"></a>설명

서식 지정 사각형은 텍스트에 대 한 제한 사각형입니다. 제한 사각형은 rich edit 컨트롤 창의 크기와는 독립적입니다. 이 `CRichEditCtrl` 개체를 처음 만들 때 서식 지정 사각형의 크기는 창의 클라이언트 영역과 같습니다. 서식 `SetRect` 지정 사각형을 서식 있는 편집 창 보다 크거나 작게 만들려면를 사용 합니다.

자세한 내용은 Windows SDK에서 [EM_SETRECT](/windows/win32/Controls/em-setrect) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#30](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_30.cpp)]

##  <a name="setsel"></a>  CRichEditCtrl::SetSel

이 `CRichEditCtrl` 개체의 선택 항목을 설정 합니다.

```
void SetSel(
    long nStartChar,
    long nEndChar);

void SetSel(CHARRANGE& cr);
```

### <a name="parameters"></a>매개 변수

*nStartChar*<br/>
선택 영역에 대 한 첫 번째 문자의 0부터 시작 하는 인덱스입니다.

*nEndChar*<br/>
선택 영역에 대 한 마지막 문자의 0부터 시작 하는 인덱스입니다.

*cr*<br/>
현재 선택 영역의 범위를 포함 하는 [Charrange](/windows/win32/api/richedit/ns-richedit-charrange) 구조체입니다.

### <a name="remarks"></a>설명

이 함수의 두 가지 형태는 선택 영역에 대 한 경계를 설정 하는 다른 방법을 제공 합니다. 이러한 폼에 대 한 간략 한 설명은 다음과 같습니다.

- **Setsel (** `cr` **)** 이 `CHARRANGE` 폼은`cpMax`및멤버가 포함 된 구조체를 사용 하 여 범위를 설정 합니다. `cpMin`

- **Setsel (** `nStartChar` **,** `nEndChar` **)** 이 양식은 *nstartchar* 및 *nendchar* 매개 변수를 사용 하 여 범위를 설정 합니다.

캐럿은 start`cpMin` (또는 *nstartchar*) 및 end (`cpMax` 또는 *nendchar*) 인덱스 중 더 큰 값으로 표시 되는 선택의 끝에 배치 됩니다. 이 함수는 캐럿이 표시 `CRichEditCtrl` 되도록의 내용을 스크롤합니다.

이 `CRichEditCtrl` 개체의 모든 텍스트를 선택 하려면 시작 인덱스 `SetSel` 0과-1의 끝 인덱스를 사용 하 여를 호출 합니다.

자세한 내용은 [EM_EXSETSEL](/windows/win32/Controls/em-exsetsel) Message And [charrange](/windows/win32/api/richedit/ns-richedit-charrange) structure in the Windows SDK를 참조 하세요.

### <a name="example"></a>예제

  [Getsel](#getsel)의 예제를 참조 하세요.

##  <a name="setselectioncharformat"></a>  CRichEditCtrl::SetSelectionCharFormat

이 `CRichEditCtrl` 개체의 현재 선택 영역에 있는 텍스트의 문자 서식 특성을 설정 합니다.

```
BOOL SetSelectionCharFormat(CHARFORMAT& cf);
BOOL SetSelectionCharFormat(CHARFORMAT2& cf);
```

### <a name="parameters"></a>매개 변수

*cf*<br/>
첫 번째 버전에서 현재 선택 영역에 대 한 새 문자 서식 특성을 포함 하는 [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-_charformat) 구조체에 대 한 포인터입니다.

두 번째 버전에서 [CHARFORMAT2](/windows/win32/api/richedit/ns-richedit-charformat2w) 구조체에 대 한 포인터로, 현재 선택 항목에 대 한 새 문자 서식 지정 `CHARFORMAT` 특성을 포함 하는 구조에 대 한 Rich Edit 2.0 확장입니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 함수는 `dwMask` *cf* 의 멤버로 지정 된 특성만 변경 합니다.

자세한 내용은 [EM_SETCHARFORMAT](/windows/win32/Controls/em-setcharformat) 및 `CHARFORMAT` Windows SDK의 및 `CHARFORMAT2` 구조를 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#31](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_31.cpp)]

##  <a name="settargetdevice"></a>  CRichEditCtrl::SetTargetDevice

이 `CRichEditCtrl` 개체의 WYSIWYG (표시 되는 항목)에 사용 되는 대상 장치 및 선 두께를 설정 합니다.

```
BOOL SetTargetDevice(
    HDC hDC,
    long lLineWidth);

BOOL SetTargetDevice(
    CDC& dc,
    long lLineWidth);
```

### <a name="parameters"></a>매개 변수

*hDC*<br/>
새 대상 장치에 대 한 장치 컨텍스트에 대 한 핸들입니다.

*lLineWidth*<br/>
서식 지정에 사용할 선 두께입니다.

*dc*<br/>
새 대상 장치에 대 한 [CDC](../../mfc/reference/cdc-class.md) .

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 함수가 성공적으로 실행 되 면 rich edit 컨트롤은 매개 변수로 전달 된 장치 컨텍스트를 소유 합니다. 이 경우 호출 함수는 장치 컨텍스트를 소멸 시 키 지 않아야 합니다.

자세한 내용은 Windows SDK에서 [EM_SETTARGETDEVICE](/windows/win32/Controls/em-settargetdevice) 을 참조 하세요.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#32](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_32.cpp)]

##  <a name="settextmode"></a>  CRichEditCtrl::SetTextMode

Rich edit 컨트롤의 텍스트 모드 또는 실행 취소 및 다시 실행 수준을 설정 합니다.

```
BOOL SetTextMode(UINT fMode);
```

### <a name="parameters"></a>매개 변수

*fMode*<br/>
컨트롤의 텍스트 모드와 실행 취소 수준 매개 변수의 새 설정을 지정 합니다. 가능한 값 목록은 Windows SDK의 [EM_SETTEXTMODE](/windows/win32/Controls/em-settextmode) 에 대 한 mode 매개 변수를 참조 하세요.

### <a name="return-value"></a>반환 값

성공 하면 0이 고, 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

텍스트 모드에 대 한 설명은 Windows SDK의 EM_SETTEXTMODE를 참조 하세요.

컨트롤에 텍스트가 포함 되어 있으면이 멤버 함수는 실패 합니다. 컨트롤이 비어 있는지 확인 하려면 빈 문자열을 포함 하는 [WM_SETTEXT](/windows/win32/winmsg/wm-settext) 메시지를 보냅니다.

##  <a name="setundolimit"></a>  CRichEditCtrl::SetUndoLimit

실행 취소 큐에 저장 될 수 있는 최대 작업 수를 설정 합니다.

```
UINT SetUndoLimit(UINT nLimit);
```

### <a name="parameters"></a>매개 변수

*nLimit*<br/>
실행 취소 큐에 저장할 수 있는 최대 작업 수를 지정 합니다. 실행 취소를 사용 하지 않으려면 0으로 설정 합니다.

### <a name="return-value"></a>반환 값

Rich edit 컨트롤에 대 한 최대 실행 취소 작업의 최대 수입니다.

### <a name="remarks"></a>설명

기본적으로 실행 취소 큐의 최대 작업 수는 100입니다. 이 값을 늘리면 새 숫자를 수용할 수 있는 충분 한 메모리가 있어야 합니다. 성능 향상을 위해 가능한 가장 작은 값으로 제한을 설정 합니다.

##  <a name="setwordcharformat"></a>  CRichEditCtrl::SetWordCharFormat

이 `CRichEditCtrl` 개체에서 현재 선택 된 단어의 문자 서식 특성을 설정 합니다.

```
BOOL SetWordCharFormat(CHARFORMAT& cf);
BOOL SetWordCharFormat(CHARFORMAT2& cf);
```

### <a name="parameters"></a>매개 변수

*cf*<br/>
첫 번째 버전에서 현재 선택 된 단어에 대 한 새 문자 서식 특성을 포함 하는 [CHARFORMAT](/windows/win32/api/richedit/ns-richedit-_charformat) 구조체에 대 한 포인터입니다.

두 번째 버전에서 [CHARFORMAT2](/windows/win32/api/richedit/ns-richedit-charformat2w) 구조체에 대 한 포인터로, 현재 선택 된 단어에 대 한 새 문자 서식 `CHARFORMAT` 지정 특성을 포함 하는 구조에 대 한 Rich Edit 2.0 extension입니다.

### <a name="return-value"></a>반환 값

성공 하면 0이 아닌 값 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

이 함수는 `dwMask` *cf* 의 멤버로 지정 된 특성만 변경 합니다.

자세한 내용은 Windows SDK [EM_SETCHARFORMAT](/windows/win32/Controls/em-setcharformat) 메시지 및 `CHARFORMAT` 및 `CHARFORMAT2` 구조체를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#33](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_33.cpp)]

##  <a name="setwordwrapmode"></a>  CRichEditCtrl::SetWordWrapMode

Rich edit 컨트롤의 단어 줄 바꿈 및 단어 분리 옵션을 설정 합니다.

```
UINT SetWordWrapMode(UINT uFlags) const;
```

### <a name="parameters"></a>매개 변수

*uFlags*<br/>
자동 줄 바꿈 및 단어 분리를 위해 설정할 옵션입니다. 사용할 수 있는 옵션 목록은 Windows SDK의 [EM_SETWORDWRAPMODE](/windows/win32/Controls/em-setwordwrapmode) 를 참조 하세요.

### <a name="return-value"></a>반환 값

현재 단어 줄 바꿈 및 단어 분리 옵션입니다.

### <a name="remarks"></a>설명

이 메시지는 아시아 언어 버전의 운영 체제 에서만 사용할 수 있습니다.

##  <a name="stopgrouptyping"></a>  CRichEditCtrl::StopGroupTyping

컨트롤에서 추가 입력 작업을 현재 실행 취소 작업으로 수집 하지 않도록 합니다.

```
void StopGroupTyping();
```

### <a name="remarks"></a>설명

컨트롤은 다음 입력 작업 (있는 경우)을 실행 취소 큐의 새 작업에 저장 합니다.

자세한 내용은 Windows SDK에서 [EM_STOPGROUPTYPING](/windows/win32/Controls/em-stopgrouptyping) 을 참조 하세요.

##  <a name="streamin"></a>  CRichEditCtrl::StreamIn

이 `CRichEditCtrl` 개체의 텍스트를 지정 된 입력 스트림의 텍스트로 바꿉니다.

```
long StreamIn(
    int nFormat,
    EDITSTREAM& es);
```

### <a name="parameters"></a>매개 변수

*nFormat*<br/>
입력 데이터 형식을 지정 하는 플래그입니다. 자세한 내용은 설명 부분을 참조하세요.

*es*<br/>
입력 스트림을 지정 하는 [editstream](/windows/win32/api/richedit/ns-richedit-editstream) 구조체입니다. 자세한 내용은 설명 부분을 참조하세요.

### <a name="return-value"></a>반환 값

입력 스트림에서 읽은 문자 수입니다.

### <a name="remarks"></a>설명

*Nformat* 의 값은 다음 중 하나 여야 합니다.

- SF_TEXT는 텍스트만 읽도록 지정 합니다.

- SF_RTF은 텍스트 읽기와 서식 지정을 나타냅니다.

이러한 값 중 하나를 SFF_SELECTION와 결합할 수 있습니다. SFF_SELECTION가 지정 `StreamIn` 된 경우는 현재 선택 영역을 입력 스트림의 내용으로 바꿉니다. 지정 `StreamIn` 하지 않으면이 `CRichEditCtrl` 개체의 전체 내용을 바꿉니다.

매개 변수에서 버퍼를 텍스트로 채우는 콜백 함수를 지정 합니다. `EDITSTREAM` 이 콜백 함수는 입력 스트림이 모두 사용 될 때까지 반복적으로 호출 됩니다.

자세한 내용은 Windows SDK [EM_STREAMIN](/windows/win32/Controls/em-streamin) 메시지 및 [editstream](/windows/win32/api/richedit/ns-richedit-editstream) structure를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#34](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_34.cpp)]

[!code-cpp[NVC_MFC_CRichEditCtrl#35](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_35.cpp)]

##  <a name="streamout"></a>  CRichEditCtrl::StreamOut

이 `CRichEditCtrl` 개체의 내용을 지정 된 출력 스트림에 씁니다.

```
long StreamOut(
    int nFormat,
    EDITSTREAM& es);
```

### <a name="parameters"></a>매개 변수

*nFormat*<br/>
출력 데이터 형식을 지정 하는 플래그입니다. 자세한 내용은 설명 부분을 참조하세요.

*es*<br/>
출력 스트림을 지정 하는 [Editstream](/windows/win32/api/richedit/ns-richedit-editstream) 구조체입니다. 자세한 내용은 설명 부분을 참조하세요.

### <a name="return-value"></a>반환 값

출력 스트림에 쓴 문자 수입니다.

### <a name="remarks"></a>설명

*Nformat* 의 값은 다음 중 하나 여야 합니다.

- SF_TEXT은 텍스트 쓰기만 나타냅니다.

- SF_RTF은 텍스트 쓰기 및 서식 지정을 나타냅니다.

- SF_RTFNOOBJS은 텍스트 및 서식 쓰기를 나타내며, OLE 항목을 공백으로 바꿉니다.

- SF_TEXTIZED는 OLE 항목의 텍스트 표시를 사용 하 여 텍스트와 서식 지정을 작성 합니다.

이러한 값은 SFF_SELECTION와 함께 사용할 수 있습니다. SFF_SELECTION를 지정 `StreamOut` 하면 현재 선택 항목을 출력 스트림에 씁니다. 지정 `StreamOut` 하지 않으면이 `CRichEditCtrl` 개체의 전체 내용을 씁니다.

매개 변수에서 버퍼를 텍스트로 채우는 콜백 함수를 지정 합니다. `EDITSTREAM` 이 콜백 함수는 출력 스트림이 모두 사용 될 때까지 반복적으로 호출 됩니다.

자세한 내용은 Windows SDK [EM_STREAMOUT](/windows/win32/Controls/em-streamout) 메시지 및 [editstream](/windows/win32/api/richedit/ns-richedit-editstream) structure를 참조 하십시오.

### <a name="example"></a>예제

[!code-cpp[NVC_MFC_CRichEditCtrl#36](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_36.cpp)]

[!code-cpp[NVC_MFC_CRichEditCtrl#37](../../mfc/reference/codesnippet/cpp/cricheditctrl-class_37.cpp)]

##  <a name="undo"></a>  CRichEditCtrl::Undo

Rich edit 컨트롤에서 마지막 작업을 실행 취소 합니다.

```
BOOL Undo();
```

### <a name="return-value"></a>반환 값

실행 취소 작업이 성공 하면 0이 아닌 값입니다. 그렇지 않으면 0입니다.

### <a name="remarks"></a>설명

실행 취소 작업을 실행 취소할 수도 있습니다. 예를 들어,에 대 `Undo`한 첫 번째 호출로 삭제 된 텍스트를 복원할 수 있습니다. 중간 편집 작업이 없는 경우에 대 `Undo`한 두 번째 호출로 텍스트를 다시 제거할 수 있습니다.

자세한 내용은 Windows SDK에서 [EM_UNDO](/windows/win32/Controls/em-undo) 을 참조 하세요.

### <a name="example"></a>예제

  [Canundo](#canundo)의 예제를 참조 하세요.

## <a name="see-also"></a>참고자료

[MFC 샘플 워드 패드](../../overview/visual-cpp-samples.md)<br/>
[CWnd 클래스](../../mfc/reference/cwnd-class.md)<br/>
[계층 구조 차트](../../mfc/hierarchy-chart.md)<br/>
[CEdit Class](../../mfc/reference/cedit-class.md)<br/>
[CRichEditView 클래스](../../mfc/reference/cricheditview-class.md)
